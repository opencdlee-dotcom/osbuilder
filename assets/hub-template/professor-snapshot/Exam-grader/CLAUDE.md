# Exam-grader

Exam grading sub-tool. Routes to `lab-grader` and `lab-test-grader` skills.

NOTE: the real `../professor/` directory uses `Exam grader/` with a space. The snapshot uses `Exam-grader/` (hyphen) so subtool names pass `_validate_project_name`'s `[a-zA-Z0-9_-]+` regex (T-07-05-01 mitigation). The STRUCTURAL contract — "one subdir per tool, each with its own CLAUDE.md" — is preserved.
