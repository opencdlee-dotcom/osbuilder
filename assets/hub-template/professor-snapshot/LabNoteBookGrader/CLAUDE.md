# LabNoteBookGrader

Notebook grading sub-tool. Routes to `canvas-notebook-grader` skill.

This subdir is part of the structural contract for the hub-platform playbook (D-05). Real implementation lives in `../professor/LabNoteBookGrader/`; this snapshot file exists only so structural-diff verification can run without a live filesystem dependency.
