# {{project_name}} — Workspace Guide

Central workspace for the tools below. Open this folder whenever you need any of them.

## Tool Routing

| Folder | Purpose | Runtime |
|--------|---------|---------|
{{routing_table}}

## Common Tasks

Per sub-tool, open the sub-tool folder and follow its local `AGENTS.md` for the
runtime-specific commands and conventions.

## Notes For LLM Agents

- Use the nearest subproject `AGENTS.md`; this file is only a router for the hub.
- Folder names are exact and case-sensitive.
- Keep secrets out of source files; prefer environment variables or Keychain
  over committed config.
