---
phase: 08-skill-quality-publish-bar
plan: 08
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - examples/README.md
  - examples/01-todo-web/SPEC.md
  - examples/01-todo-web/repo-url.txt
  - examples/01-todo-web/screenshots/.gitkeep
  - examples/02-cli-photo-organizer/SPEC.md
  - examples/02-cli-photo-organizer/repo-url.txt
  - examples/02-cli-photo-organizer/screenshots/.gitkeep
  - examples/03-fastapi-summarizer/SPEC.md
  - examples/03-fastapi-summarizer/repo-url.txt
  - examples/03-fastapi-summarizer/screenshots/.gitkeep
autonomous: true
requirements: [QUAL-04]
must_haves:
  truths:
    - "examples/ contains exactly 3 example sub-directories (the QUAL-04 minimum; gallery can grow to 5 in future)"
    - "Each sub-dir has a SPEC.md with the original paragraph + inferred playbook + before/after structure"
    - "Each sub-dir has repo-url.txt (real URL or NOT_PUBLISHED placeholder for not-yet-built examples)"
    - "Each sub-dir has screenshots/ directory (with .gitkeep so it's tracked even before screenshots land)"
    - "examples/ covers >= 3 distinct playbooks (Pitfall 5: no 5-TODOs filler) — chosen: web, cli, ai-service"
    - "examples/README.md indexes the 3 examples with a playbook column"
    - "Wave 0 stubs in scripts/tests/test_examples.py flip RED → GREEN (4/4 tests passing)"
  artifacts:
    - path: "examples/README.md"
      provides: "Gallery index + adding-a-new-example workflow"
      min_lines: 30
    - path: "examples/01-todo-web/SPEC.md"
      provides: "Web playbook example"
      contains: "web"
    - path: "examples/02-cli-photo-organizer/SPEC.md"
      provides: "CLI playbook example"
      contains: "cli"
    - path: "examples/03-fastapi-summarizer/SPEC.md"
      provides: "AI-service playbook example"
      contains: "ai-service"
  key_links:
    - from: "examples/README.md"
      to: "examples/<name>/SPEC.md"
      via: "table-of-contents row per example"
      pattern: "examples/[0-9]+-[a-z-]+/SPEC\\.md"
    - from: "README.md (08-06)"
      to: "examples/README.md"
      via: "gallery cross-link in `## Examples` section"
      pattern: "examples/"
---

<objective>
Populate the examples gallery with 3 reference apps covering 3 distinct playbooks
(web, cli, ai-service). Each example: SPEC.md (intake paragraph + playbook +
before/after) + repo-url.txt (real URL or NOT_PUBLISHED) + screenshots/ dir.

Purpose: QUAL-04 requires `examples/` contains >= 3 (target 5) reference apps.
This plan ships the QUAL-04 minimum with NOT_PUBLISHED placeholders for repos
that have not yet been built; future re-runs can flip placeholders to real URLs
as OSBuilder produces actual gallery candidates.

**Important:** Per 08-RESEARCH.md Assumption A3, Phase 6 (ship-to-private-github)
is currently 0/6 plans complete. Real OSBuilder builds with real GitHub URLs
cannot exist until Phase 6 + 7 produce them. This plan ships SPEC.md text + the
NOT_PUBLISHED placeholder workflow so the gallery is structurally complete; the
manual UAT (08-HUMAN-UAT.md row 4) tracks when each example is upgraded from
"placeholder" to "real built" status.

Output:
- examples/README.md (gallery index)
- 3 example sub-directories with SPEC.md + repo-url.txt + screenshots/
- Wave 0 stubs in test_examples.py flip to GREEN (4/4 tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md
@.planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md
@references/playbooks/web.md
@references/playbooks/cli.md
@references/playbooks/ai-service.md
@references/markers.md
@scripts/tests/test_examples.py

<interfaces>
<!-- Test contract from test_examples.py (08-01 stubs) -->
- examples/ directory must contain >= 3 sub-directories (excluding hidden)
- Each sub-dir must have a SPEC.md (file)
- Sum of distinct playbooks mentioned in SPEC.md files >= 3 (case-insensitive substring match against EXPECTED_PLAYBOOKS = ("web", "ai-service", "cli", "desktop", "hub-platform"))
- Each sub-dir must have repo-url.txt with content matching `^https://github\.com/` OR exactly `NOT_PUBLISHED`

<!-- Playbook reference: which example uses which -->
- example 01-todo-web → references/playbooks/web.md (Next.js + Drizzle + Postgres + Tailwind)
- example 02-cli-photo-organizer → references/playbooks/cli.md (Python + Typer + Rich + SQLite)
- example 03-fastapi-summarizer → references/playbooks/ai-service.md (FastAPI + uv + Pydantic v2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples/README.md gallery index</name>
  <files>examples/README.md</files>
  <action>
    Read `examples/.gitkeep` to confirm `examples/` exists. Create `examples/README.md` using 08-PATTERNS.md "examples/README.md" section as the verbatim template.

    Content:

    ```markdown
    # OSBuilder Examples Gallery

    Real apps OSBuilder built end-to-end from a plain-English paragraph. Each
    example shows the original spec, before/after screenshots (where available),
    and a link to the resulting (private or public-mirror) GitHub repo.

    > **Pitfall 5 guard:** every example uses a different playbook to demonstrate
    > OSBuilder's range. We refuse "5 TODO-list variants" filler.

    ## Current examples

    | # | Example | Playbook | Spec | Repo |
    |---|---------|----------|------|------|
    | 1 | TODO web app | web | [SPEC.md](01-todo-web/SPEC.md) | [repo-url.txt](01-todo-web/repo-url.txt) |
    | 2 | Photo library CLI organizer | cli | [SPEC.md](02-cli-photo-organizer/SPEC.md) | [repo-url.txt](02-cli-photo-organizer/repo-url.txt) |
    | 3 | FastAPI document summarizer | ai-service | [SPEC.md](03-fastapi-summarizer/SPEC.md) | [repo-url.txt](03-fastapi-summarizer/repo-url.txt) |

    ## Repo URL placeholders

    A `repo-url.txt` containing `NOT_PUBLISHED` means the example's SPEC has been
    documented but a real OSBuilder build has not yet produced a GitHub repo for
    it (e.g., the build is gated on Phase 6 / Phase 7 completion of OSBuilder
    itself). The manual UAT in
    `.planning/phases/08-skill-quality-publish-bar/08-HUMAN-UAT.md` row 4 tracks
    the upgrade-to-real-URL milestone.

    Once a real OSBuilder build produces a repo URL, replace the placeholder
    with the literal URL (must match `^https://github\\.com/`).

    ## Adding a new example

    1. Run OSBuilder against a paragraph describing the new app:
       `/osbuilder I want a <description>`
    2. Note the inferred playbook (from `state.md` `playbook` field or from
       OSBuilder's narration).
    3. Create the example directory: `examples/NN-slug/` (NN = next number,
       zero-padded; slug = short kebab-case description)
    4. Copy the resulting derived_spec.md → `examples/NN-slug/SPEC.md`; flesh
       out the before/after sections with screenshots.
    5. Capture 2-3 screenshots → `examples/NN-slug/screenshots/`. Keep PNGs
       compressed (<= 200KB each) per Pitfall in 08-RESEARCH.md.
    6. Write the resulting repo URL into `examples/NN-slug/repo-url.txt`. If
       the repo is private and you don't want to publish a mirror yet, use
       `NOT_PUBLISHED`.
    7. Add a row to the table above, preserving the playbook-distinctness rule
       (Pitfall 5).

    ## Target

    QUAL-04 minimum is 3 examples; target is 5. The next two candidate
    playbooks for examples 4 and 5 are:

    - **04-tauri-tray-app** (desktop playbook — Tauri 2 + React)
    - **05-hub-platform-mini** (hub-platform — top-level CLAUDE.md routing)

    These are deferred because Phase 6 (ship-to-private-github) and Phase 7
    (additional playbooks) need to ship before real built apps exist for desktop
    and hub-platform; until then the SPEC.md alone would not have a real repo
    to point to.

    ## See also

    - [`../README.md`](../README.md) — main project README (links to this gallery)
    - [`../references/playbooks/`](../references/playbooks/) — per-playbook scaffolder docs
    - [`../.planning/phases/08-skill-quality-publish-bar/08-HUMAN-UAT.md`](../.planning/phases/08-skill-quality-publish-bar/08-HUMAN-UAT.md) row 4 — manual UAT for "examples were really built"
    ```
  </action>
  <read_first>
    - examples/ (confirm directory exists; only .gitkeep currently inside)
    - references/markers.md (table-of-contents analog with notes on placeholders)
    - .planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md (examples/README.md section, lines 640-668)
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Pitfall 5 distinct-playbooks rule; Assumption A3 phase-completion gating)
  </read_first>
  <acceptance_criteria>
    - File `examples/README.md` exists
    - File line count >= 30
    - File contains `# OSBuilder Examples Gallery` H1
    - File contains a markdown table with `| Playbook |` column header
    - File contains exactly 3 example rows in the table (greppable by `| 01-todo-web/SPEC.md |` style)
    - File contains the literal `Pitfall 5` reference
    - File contains the `NOT_PUBLISHED` placeholder explanation section
    - File contains `## Adding a new example` workflow section
  </acceptance_criteria>
  <verify>
    <automated>test -f examples/README.md && wc -l examples/README.md | awk '{exit ($1 >= 30) ? 0 : 1}' && grep -q "^# OSBuilder Examples Gallery" examples/README.md && grep -q "| Playbook |" examples/README.md && [ "$(grep -cE "examples/0[0-9]+-[a-z-]+/SPEC\\.md" examples/README.md)" -ge "3" ] && grep -q "Pitfall 5" examples/README.md && grep -q "NOT_PUBLISHED" examples/README.md && grep -q "^## Adding a new example" examples/README.md</automated>
  </verify>
  <done>
    examples/README.md exists with 3-row gallery table, NOT_PUBLISHED policy, and add-new-example workflow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 3 example sub-directories with SPEC.md + repo-url.txt + screenshots/</name>
  <files>
    examples/01-todo-web/SPEC.md,
    examples/01-todo-web/repo-url.txt,
    examples/01-todo-web/screenshots/.gitkeep,
    examples/02-cli-photo-organizer/SPEC.md,
    examples/02-cli-photo-organizer/repo-url.txt,
    examples/02-cli-photo-organizer/screenshots/.gitkeep,
    examples/03-fastapi-summarizer/SPEC.md,
    examples/03-fastapi-summarizer/repo-url.txt,
    examples/03-fastapi-summarizer/screenshots/.gitkeep
  </files>
  <action>
    For each of the 3 example sub-directories, create:
    - `SPEC.md` (per-app brief; structure from 08-PATTERNS.md "examples/<name>/SPEC.md" section)
    - `repo-url.txt` (one line: either a real URL or `NOT_PUBLISHED`)
    - `screenshots/.gitkeep` (empty)

    `mkdir -p examples/01-todo-web/screenshots examples/02-cli-photo-organizer/screenshots examples/03-fastapi-summarizer/screenshots`

    **All 3 SPEC.md files MUST contain the literal playbook name as a substring** (the test_examples.py:test_distinct_playbooks scan greps the SPEC.md text case-insensitively for "web", "cli", or "ai-service").

    ---

    **File 1: `examples/01-todo-web/SPEC.md`**

    ```markdown
    # Example 01: TODO web app

    **Playbook:** web
    **Built:** <pending — placeholder example until OSBuilder Phase 6+7 complete>
    **Repo:** [repo-url.txt](repo-url.txt)

    ## Original paragraph (intake)

    > "I want a TODO web app where I can add tasks and check them off. It should
    > work on my phone too, and I want my tasks to still be there if I close the
    > browser and come back tomorrow."

    ## OSBuilder's expected interpretation

    - **App type:** web
    - **Playbook:** `references/playbooks/web.md` (Next.js 16.x + Drizzle +
      Postgres + Tailwind 4 + pnpm)
    - **Inferred from keywords:** "web app", "phone too" (responsive), "still be
      there tomorrow" (persistence)
    - **Notable refusals:** none in this example
    - **Production-ready flag:** OFF (default — single-user TODO app does not
      need OpenTelemetry / Sentry / etc.)

    ## Before / After

    | Stage | What it looked like | Screenshot |
    |-------|---------------------|------------|
    | Intake (paragraph) | (the paragraph above) | — |
    | Derived spec | structured features list + stack hints | `screenshots/derived-spec.png` (pending) |
    | Scaffolded project | Next.js + Drizzle + Postgres tree | `screenshots/scaffold-tree.png` (pending) |
    | Working app | http://localhost:3000 with TODO list rendering | `screenshots/running.png` (pending) |
    | Private GitHub | `gh repo create --private` succeeds; URL shown | `screenshots/repo-view.png` (pending) |

    ## How OSBuilder built this (expected sequence)

    1. **PM** gathered requirements from the paragraph; asked one clarifying
       question ("Should other people be able to share your TODO list, or just
       you?" — user picks "just me", which sets `app_type: web` + `multi_user:
       false` (Postgres still appropriate per Phase 3 SC-4).
    2. **Architect** chose Next.js 16.x + Drizzle + Postgres + Tailwind 4 + pnpm
       (per `references/playbooks/web.md`).
    3. **DevOps** ran `pnpm create next-app@latest` then wired Drizzle schema +
       compose.yaml.
    4. **Frontend** built the TODO list page; **Backend** wired the persistence
       layer; **DevOps** stamped Dockerfile + .github/workflows/test.yml.
    5. **QA** ran `/code-tester` adversarial tests; **Reviewer** ran `/predator`
       + `/gsd:code-review`.
    6. **Tech Writer** stamped a clone-and-run README via `/gsd:docs-update` +
       `/humanizer`.
    7. **DevOps** ran `gh repo create --private` and pushed.

    ## Status

    This example is currently a SPEC placeholder. It will be upgraded to a real
    built repo once OSBuilder Phase 6 (ship-to-private-github) and Phase 7
    (additional playbooks) complete and a real run produces a real URL.
    `repo-url.txt` will be flipped from `NOT_PUBLISHED` to the real URL at that
    point.

    ## See also

    - [`../../references/playbooks/web.md`](../../references/playbooks/web.md) — web playbook reference
    - [`../README.md`](../README.md) — gallery index
    ```

    **File 2: `examples/01-todo-web/repo-url.txt`** (one line, no trailing whitespace beyond a final newline):

    ```
    NOT_PUBLISHED
    ```

    **File 3: `examples/01-todo-web/screenshots/.gitkeep`** (empty)

    ---

    **File 4: `examples/02-cli-photo-organizer/SPEC.md`**

    ```markdown
    # Example 02: Photo library CLI organizer

    **Playbook:** cli
    **Built:** <pending — placeholder example until OSBuilder Phase 6+7 complete>
    **Repo:** [repo-url.txt](repo-url.txt)

    ## Original paragraph (intake)

    > "I want a command-line tool to organize my photo library. It should be
    > able to scan a folder, tag photos by date, and let me search them later.
    > It should remember what it tagged across runs."

    ## OSBuilder's expected interpretation

    - **App type:** cli
    - **Playbook:** `references/playbooks/cli.md` (Python + Typer + Rich + SQLite)
    - **Inferred from keywords:** "command-line tool" (cli), "remember across
      runs" (SQLite persistence per Phase 7 SC-2)
    - **Notable refusals:** none — single-user local tool fits the cli playbook
      exactly (no Dockerfile per CLI playbook decision in 07-03)
    - **Production-ready flag:** OFF (single-user local tool)

    ## Before / After

    | Stage | What it looked like | Screenshot |
    |-------|---------------------|------------|
    | Intake (paragraph) | (the paragraph above) | — |
    | Derived spec | features list + Python + Typer stack | `screenshots/derived-spec.png` (pending) |
    | Scaffolded project | uv project + typer + rich + sqlite | `screenshots/scaffold-tree.png` (pending) |
    | Working tool | `uv run photo-organizer scan ~/Pictures` | `screenshots/running.png` (pending) |
    | SQLite persistence | second run shows tagged-photo recall | `screenshots/persistence.png` (pending) |
    | Private GitHub | `gh repo create --private` URL | `screenshots/repo-view.png` (pending) |

    ## How OSBuilder built this (expected sequence)

    1. **PM** gathered requirements; confirmed single-user (no shared library);
       asked about photo formats (sensible default: JPEG + PNG + HEIC).
    2. **Architect** chose Python + Typer + Rich + SQLite per the CLI playbook;
       NO Dockerfile per CLI playbook refuse-list (single-user local tool).
    3. **DevOps** ran `uv init` + `uv add typer rich` (NOT `typer[all]` per
       07-03 D-13/D-14 — rich is hard-deped from typer 0.25.1+).
    4. **Frontend** N/A (CLI tool — no frontend role in this build).
       **Backend** wired SQLite schema + the `scan` / `tag` / `search`
       subcommands; **DevOps** stamped python.yml CI workflow (no Dockerfile).
    5. **QA** + **Reviewer** as usual.
    6. **Tech Writer** wrote README documenting the CLI surface in plain English.
    7. **DevOps** pushed to private GitHub.

    ## Status

    This example is currently a SPEC placeholder. It will be upgraded to a real
    built repo once OSBuilder Phase 6 (ship-to-private-github) completes and a
    real run produces a real URL. `repo-url.txt` will be flipped from
    `NOT_PUBLISHED` to the real URL at that point.

    ## See also

    - [`../../references/playbooks/cli.md`](../../references/playbooks/cli.md) — cli playbook reference
    - [`../README.md`](../README.md) — gallery index
    ```

    **File 5: `examples/02-cli-photo-organizer/repo-url.txt`**:

    ```
    NOT_PUBLISHED
    ```

    **File 6: `examples/02-cli-photo-organizer/screenshots/.gitkeep`** (empty)

    ---

    **File 7: `examples/03-fastapi-summarizer/SPEC.md`**

    ```markdown
    # Example 03: FastAPI document summarizer

    **Playbook:** ai-service
    **Built:** <pending — placeholder example until OSBuilder Phase 6+7 complete>
    **Repo:** [repo-url.txt](repo-url.txt)

    ## Original paragraph (intake)

    > "I want an HTTP API that summarizes documents with an LLM. Other tools
    > should be able to POST a document and get back a 200-word summary. I want
    > it to run locally on my machine."

    ## OSBuilder's expected interpretation

    - **App type:** ai-service
    - **Playbook:** `references/playbooks/ai-service.md` (FastAPI + uv +
      Pydantic v2 + `assets/fastapi-starter/`)
    - **Inferred from keywords:** "HTTP API" (ai-service), "summarize with an
      LLM" (LLM-shaped endpoint), "POST a document" (REST contract).
    - **Notable refusals:** none. (User did not request K8s, microservices, or
      multi-region deploy — defaults apply.)
    - **Production-ready flag:** OFF — single-user local API does not need
      OpenTelemetry / Sentry / rate-limiting in default mode.

    ## Before / After

    | Stage | What it looked like | Screenshot |
    |-------|---------------------|------------|
    | Intake (paragraph) | (the paragraph above) | — |
    | Derived spec | endpoint contract + Pydantic v2 schemas | `screenshots/derived-spec.png` (pending) |
    | Scaffolded project | uv + fastapi-starter + pydantic v2 | `screenshots/scaffold-tree.png` (pending) |
    | Working app | `uv run fastapi dev` boots `/docs` page | `screenshots/docs-page.png` (pending) |
    | /summarize smoke | `curl -X POST /summarize -d ...` returns summary | `screenshots/summarize-200.png` (pending) |
    | Pydantic v2 validation | empty-text request returns 422 (Pitfall 4) | `screenshots/422-error.png` (pending) |
    | Private GitHub | `gh repo create --private` URL | `screenshots/repo-view.png` (pending) |

    ## How OSBuilder built this (expected sequence)

    1. **PM** gathered requirements; asked one clarifying question ("Where will
       the summarized text come from? — you'll send it in the request, the API
       will read it from a URL, or you'll upload a file"). User picks "send it
       in the request"; `multi_user: false` (single-user local).
    2. **Architect** chose FastAPI + uv + Pydantic v2 + the OSBuilder-supplied
       `assets/fastapi-starter/` template (since no `create-fastapi-app`
       exists). Locked Pydantic v2 syntax (NOT v1) per Phase 7 Pitfall 4.
    3. **DevOps** ran `uv init` + `uv add fastapi[standard]` (brackets
       preserved as a single argv token per 07-02 D-21 implementation).
    4. **Backend** built the `/summarize` POST endpoint with Pydantic v2
       `Field(min_length=1)` validation; **Frontend** N/A.
    5. **DevOps** stamped Dockerfile + python.yml CI workflow.
    6. **QA** ran `/code-tester` (adversarial: empty body → 422; unicode →
       passes; oversize body → expected to be truncated). **Reviewer** ran
       `/predator` + `/gsd:code-review`.
    7. **Tech Writer** wrote README documenting `uv run fastapi dev` + the
       endpoint contract.
    8. **DevOps** pushed to private GitHub.

    ## Status

    This example is currently a SPEC placeholder. It will be upgraded to a real
    built repo once OSBuilder Phase 6 (ship-to-private-github) completes and a
    real run produces a real URL. `repo-url.txt` will be flipped from
    `NOT_PUBLISHED` to the real URL at that point.

    ## See also

    - [`../../references/playbooks/ai-service.md`](../../references/playbooks/ai-service.md) — ai-service playbook reference
    - [`../README.md`](../README.md) — gallery index
    ```

    **File 8: `examples/03-fastapi-summarizer/repo-url.txt`**:

    ```
    NOT_PUBLISHED
    ```

    **File 9: `examples/03-fastapi-summarizer/screenshots/.gitkeep`** (empty)
  </action>
  <read_first>
    - examples/.gitkeep (confirm examples/ dir state)
    - references/playbooks/web.md (web playbook reference for example 01)
    - references/playbooks/cli.md (cli playbook reference for example 02)
    - references/playbooks/ai-service.md (ai-service playbook reference for example 03)
    - .planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md (examples/<name>/SPEC.md section, lines 672-709)
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Pitfall 5 distinct-playbooks; Assumption A3 placeholder rationale)
    - scripts/tests/test_examples.py (test contract: SPEC.md must contain literal playbook substring; repo-url.txt must be `^https://github\.com/` or exactly `NOT_PUBLISHED`)
  </read_first>
  <acceptance_criteria>
    - 3 directories exist: `examples/01-todo-web/`, `examples/02-cli-photo-organizer/`, `examples/03-fastapi-summarizer/`
    - Each has a SPEC.md file (3 total)
    - Each has a repo-url.txt file (3 total) with content exactly `NOT_PUBLISHED` (or a `https://github.com/...` URL if user pre-built one)
    - Each has a screenshots/ directory containing .gitkeep
    - SPEC.md files contain the literal playbook substring (case-insensitive): example 01 contains "web", example 02 contains "cli", example 03 contains "ai-service"
    - SPEC.md files cover 3 DISTINCT playbooks (Pitfall 5 — verified by 4 separate greps)
    - All 4 Wave 0 tests pass: `uv run pytest scripts/tests/test_examples.py -x` reports 4 passed (no skips)
    - Total new examples files = 9 (3 SPEC.md + 3 repo-url.txt + 3 .gitkeep)
  </acceptance_criteria>
  <verify>
    <automated>for d in examples/01-todo-web examples/02-cli-photo-organizer examples/03-fastapi-summarizer; do test -f "$d/SPEC.md" || { echo "missing $d/SPEC.md"; exit 1; }; test -f "$d/repo-url.txt" || { echo "missing $d/repo-url.txt"; exit 1; }; test -f "$d/screenshots/.gitkeep" || { echo "missing $d/screenshots/.gitkeep"; exit 1; }; done && grep -qi "web" examples/01-todo-web/SPEC.md && grep -qi "cli" examples/02-cli-photo-organizer/SPEC.md && grep -qi "ai-service" examples/03-fastapi-summarizer/SPEC.md && for f in examples/0[0-9]*-*/repo-url.txt; do content="$(cat $f | tr -d '[:space:]')"; case "$content" in https://github.com/*|NOT_PUBLISHED) ;; *) echo "$f has invalid content: $content"; exit 1 ;; esac; done && uv run pytest scripts/tests/test_examples.py -x -v</automated>
  </verify>
  <done>
    3 example sub-directories committed with SPEC.md (covering web/cli/ai-service playbooks), repo-url.txt (NOT_PUBLISHED placeholders or real URLs), and screenshots/.gitkeep. 4/4 Wave 0 tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SPEC.md → public repo | Anything committed becomes public when the repo publishes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-25 | Information Disclosure | SPEC.md accidentally embeds a real user's data | mitigate | All 3 SPEC examples use canonical paragraphs from Phase 3/7 success criteria — no real user data embedded |
| T-08-26 | Information Disclosure | repo-url.txt accidentally points to a private repo URL the user did not intend to publish | mitigate | All 3 placeholder values are `NOT_PUBLISHED`; future flip-to-real-URL happens during 08-HUMAN-UAT.md row 4 (manual verification) |
| T-08-27 | Repudiation | Future maintainer commits 5MB PNGs to screenshots/ and bloats the repo | accept | examples/README.md documents the 200KB-cap convention; gitleaks pre-commit hook (Phase 6) does not check size; future tooling can add a size-check pre-commit hook if abuse becomes real |
</threat_model>

<verification>
- 3 example sub-directories each with SPEC.md + repo-url.txt + screenshots/.gitkeep
- 3 distinct playbooks covered (web, cli, ai-service) — Pitfall 5 satisfied
- examples/README.md indexes all 3 with playbook column
- 4/4 Wave 0 tests in test_examples.py GREEN
</verification>

<success_criteria>
- QUAL-04 success criterion 4: "examples/ contains at least 3 (target: 5) reference apps OSBuilder built — each with screenshots, before/after spec, and the GitHub URL of the resulting private repo (or a public mirror for the gallery)" — minimum 3 met; screenshots are .gitkeep placeholders flagged for upgrade in 08-HUMAN-UAT.md row 4; repo-url.txt uses NOT_PUBLISHED placeholders flagged for the same upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-quality-publish-bar/08-08-SUMMARY.md` documenting:
- 3 examples created with playbook coverage
- repo-url.txt status (all NOT_PUBLISHED initially; upgrade path documented)
- Pytest results (4/4 passing)
- 08-HUMAN-UAT.md row 4 status: pending until real builds replace placeholders
</output>
