---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - SKILL.md
  - references/README.md
autonomous: true
requirements: [FOUND-01, FOUND-02]
tags: [skill-md, frontmatter, progressive-disclosure]

must_haves:
  truths:
    - "After commit, `~/.claude/skills/osbuilder/SKILL.md` (once installed by Plan 03's install.sh which COPIES it) has a YAML frontmatter block with at minimum `name: osbuilder` and `description:` populated"
    - "`wc -l SKILL.md` reports ≤ 200"
    - "SKILL.md body links to at least one path under `references/`"
    - "SKILL.md contains a `## Resume Protocol` section AFTER the closing `---` frontmatter delimiter, instructing Claude to invoke `python ~/.claude/skills/osbuilder/scripts/state_writer.py read` when state.md exists — required by Phase 1 SC #5"
    - "No `<` characters appear inside the YAML frontmatter block (BLOCKER 2 / WARNING 2 mitigation — angle brackets in `<project-root>` placeholders only appear in the body, post-frontmatter)"
    - "Phase 1 success criterion #1 (frontmatter valid) and #5 (resume protocol present) are both satisfied by this plan alone"
  artifacts:
    - path: "SKILL.md"
      provides: "Skill manifest + orchestrator routing table + Resume Protocol"
      contains: "name: osbuilder"
      max_lines: 200
    - path: "references/README.md"
      provides: "Phase-1 seed reference file demonstrating the progressive-disclosure pattern is wired (resolves Assumption A10 in RESEARCH.md)"
      contains: "playbooks"
  key_links:
    - from: "SKILL.md"
      to: "references/README.md"
      via: "markdown link or routing table row"
      pattern: "references/"
    - from: "SKILL.md"
      to: "~/.claude/skills/osbuilder/scripts/state_writer.py"
      via: "Resume Protocol invocation (post-install path; BLOCKER 2 fix)"
      pattern: "state_writer\\.py"
---

<objective>
Create OSBuilder's SKILL.md (the orchestrator entrypoint that Claude Code pre-loads) plus a seed `references/README.md` to wire the progressive-disclosure pattern. Per RESEARCH.md, this is patterned after `~/.claude/skills/gsd/SKILL.md` (139 lines — closest in-ecosystem analog). The body is intentionally a thin routing table; per-playbook content fills in across Phases 3-7.

Per BLOCKER 2 resolution: Resume Protocol references the **installed** path (`~/.claude/skills/osbuilder/scripts/state_writer.py`), not the repo-root path, because non-developers run `/osbuilder` from a fresh Claude Code session that only has the install location available. install.sh (Plan 03) copies SKILL.md and scripts/ into that location, making the path real on the user's machine.

Purpose: Without a valid SKILL.md, Claude Code never recognizes the skill. Without the Resume Protocol section, Phase 1 Success Criterion #5 fails. Without the references/ link, Phase 1 Success Criterion FOUND-02 ("routes via progressive disclosure to references/") fails.

Output: 1 SKILL.md ≤ 200 lines, 1 references/README.md, three SKILL.md tests turn GREEN (`test_frontmatter_valid`, `test_skill_md_line_count_under_200`, `test_has_references_link`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Canonical SKILL.md template to mirror (cited as "best in ecosystem" by RESEARCH.md): -->
<!-- File: ~/.claude/skills/gsd/SKILL.md (139 lines, orchestrator pattern, routing table format) -->

Frontmatter shape (per RESEARCH.md Pattern 1, Anthropic-spec verbatim):
```yaml
---
name: osbuilder                    # [a-z0-9-]{1,64}, no "anthropic"/"claude", no XML
description: >                     # third-person, ≤1024 chars, includes "Triggers on: ..."
  Builds a working full-stack app and pushes it to a private GitHub repo from a
  plain-English description. Orchestrates GSD, brainiac, predator, code-tester
  via a virtual dev-team metaphor. Triggers on: /osbuilder, build me an app,
  scaffold a project, "I want a website/CLI/desktop app that…".
version: 1.0.0
user-invocable: true
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Agent, SlashCommand
argument-hint: "[describe-app] — e.g., /osbuilder I want a site where students upload lab notebooks"
---
```

Routing table format (verbatim from gsd/SKILL.md):
```markdown
| Command(s) | Reference file |
|-----------|----------------|
| `new-project`, `map-codebase` | `references/discovery.md` |
```

Resume Protocol path contract (BLOCKER 2):
```
python ~/.claude/skills/osbuilder/scripts/state_writer.py read --project-root <project-root>
```
This path is correct AFTER install.sh (Plan 03) copies scripts/state_writer.py from the
repo root into ~/.claude/skills/osbuilder/scripts/. Do NOT reference the repo-root path
in this section — non-developers running /osbuilder from a fresh Claude Code session
only have the install location available.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SKILL.md (≤200 lines, valid frontmatter, Resume Protocol)</name>
  <files>SKILL.md</files>
  <behavior>
    - Frontmatter parses with `re.match(r"^---\n(.*?)\n---\n", text, re.DOTALL)` (closed by `---` on its own line, body in between)
    - Frontmatter `name` matches `^[a-z0-9-]{1,64}$` and equals `osbuilder`
    - Frontmatter `description` ≤ 1024 chars, contains no `<` characters, no "anthropic"/"claude" reserved words
    - File line count ≤ 200 (per `wc -l SKILL.md`)
    - File body contains at least one `references/` link
    - File contains a heading `## Resume Protocol` (placed AFTER the closing `---` of the frontmatter — this is the post-frontmatter region, where `<project-root>` placeholders are allowed)
    - The Resume Protocol body mentions the installed path: `~/.claude/skills/osbuilder/scripts/state_writer.py read` (BLOCKER 2)
    - The frontmatter region (between the two `---` delimiters) contains ZERO `<` characters
  </behavior>
  <read_first>
    - ~/.claude/skills/gsd/SKILL.md (REFERENCE TEMPLATE — 139 lines, the canonical orchestrator pattern; mirror its structure: frontmatter → one-paragraph problem statement → routing table → cross-cutting rules → directory note)
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 263-364 "Pattern 1" + "Pattern 2" — frontmatter rules verbatim from Anthropic, routing-table format)
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 528-543 "Resume Protocol" — exact instructions Claude must follow on resume)
    - .planning/PROJECT.md (vision, refuse-list, locked decisions to surface in cross-cutting rules)
    - .planning/research/ARCHITECTURE.md (lines 411-428 Role-to-Skill Mapping table — paste-source for the routing table)
    - scripts/tests/test_skill_md.py (the 3 tests this file must pass; treat them as the contract)
  </read_first>
  <action>
    Create `SKILL.md` at the repo root. Target ~140-180 lines (well under the 200 cap). Use this exact section ordering. **CRITICAL — section ordering per BLOCKER 2 / WARNING 2:** The Resume Protocol section comes AFTER the closing `---` frontmatter delimiter. `<project-root>` placeholder text appears ONLY in the post-frontmatter body, never inside the YAML description value (where it would violate the no-`<` rule).

    **1. YAML frontmatter** (close with `---` on its own line). The description block contains NO `<` characters:

    ```yaml
    ---
    name: osbuilder
    description: >
      Builds a working full-stack app and pushes it to a private GitHub repo from a
      plain-English description. Orchestrates GSD, brainiac, predator, code-tester via
      a virtual dev-team metaphor (PM, Architect, Frontend, Backend, DevOps, QA,
      Reviewer, Tech Writer). Triggers on: /osbuilder, build me an app, scaffold a
      project, "I want a website/CLI/desktop app that…".
    version: 1.0.0
    user-invocable: true
    allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Agent, SlashCommand
    argument-hint: "[describe-app] — e.g., /osbuilder I want a site where students upload lab notebooks"
    ---
    ```

    The closing `---` MUST exist on its own line. Everything ABOVE the closing `---` is the constrained-no-`<` zone. Everything BELOW the closing `---` is the body — `<project-root>`, `<file>`, etc. placeholders ARE permitted there.

    **2. One-paragraph problem statement.** Mirror gsd/SKILL.md's first section. Example:

    > # OSBuilder — From a plain-English description to a private GitHub repo
    >
    > OSBuilder is the orchestrator that turns "I want a site where students upload lab notebooks" into a working, scaffolded, version-controlled app on private GitHub — without the user ever touching a command line. It composes existing skills (`gsd`, `brainiac`, `predator`, `code-tester`, `problem-solver`, `gsd-debug`) via a virtual dev-team metaphor: PM gathers requirements, Architect chooses a stack, Frontend/Backend/DevOps build, QA verifies, Reviewer audits, Tech Writer documents. Single-threaded by design — no parallel agents. State lives on disk in `<project-root>/.planning/osbuilder/state.md` so a `/clear`'d session can resume mid-build.

    **3. Build status note.** Per RESEARCH.md Pitfall 10 mitigation, list which sections are real vs forward-looking:

    ```markdown
    ## Build status (as of v1.0.0)

    | Phase | Status | What's wired |
    |-------|--------|--------------|
    | 1 — Foundation | ✅ DONE | SKILL.md, state_writer.py, bootstrap shims, install.sh |
    | 2 — Pre-flight installer | ⏳ planned | Routes below marked `(Phase 2)` are stubs |
    | 3 — Intake + Web playbook | ⏳ planned | Routes below marked `(Phase 3)` are stubs |
    | 4 — GSD handoff + Verify | ⏳ planned | Routes below marked `(Phase 4)` are stubs |
    | 5 — UX polish | ⏳ planned | Routes below marked `(Phase 5)` are stubs |
    | 6 — Ship | ⏳ planned | Routes below marked `(Phase 6)` are stubs |
    | 7 — More playbooks | ⏳ planned | Routes below marked `(Phase 7)` are stubs |
    | 8 — Publish bar | ⏳ planned | install.sh one-liner + examples gallery |

    Reference files marked `(Phase N)` are stubbed; they fill in as those phases ship.
    ```

    **4. Resume Protocol section** (REQUIRED — Phase 1 SC #5 depends on this section existing; placed AFTER the frontmatter `---` close per BLOCKER 2 / WARNING 2):

    ```markdown
    ## Resume Protocol

    Every invocation begins here. If `<project-root>/.planning/osbuilder/state.md` exists, this is a resume; if not, this is a new build.

    1. Locate the project root: walk up from cwd looking for `.planning/`, OR use the user-supplied `--project-root` argument.
    2. If `<project-root>/.planning/osbuilder/state.md` exists, run:
       ```bash
       python ~/.claude/skills/osbuilder/scripts/state_writer.py read --project-root <project-root>
       ```
       and resume from the printed `next_action` field. Do not re-run intake; do not reset `retry_count`.
    3. If state.md does not exist, this is a new build → run intake (Phase 3 deliverable; until then, prompt the user with: "OSBuilder Phase 1 only ships state plumbing. Phase 3 wires intake.").

    The `state.md` schema is 10 named fields plus `updated_at`. See `references/state-schema.md` (Phase 4 deliverable) or read `~/.claude/skills/osbuilder/scripts/state_writer.py --help`.
    ```

    Note: the path `~/.claude/skills/osbuilder/scripts/state_writer.py` is the INSTALLED location. Plan 03's install.sh COPIES `scripts/state_writer.py` from the repo root into this location. Non-developers running `/osbuilder` from a fresh Claude Code session only see the install location, so the absolute installed path is what we surface here (BLOCKER 2 resolution).

    **5. Routing table** (categorical, not per-command — per RESEARCH.md Pitfall 2):

    ```markdown
    ## Command Routing

    OSBuilder routes by intent. Each row maps a category to the reference file (or script) that contains the full instructions.

    | Category | Triggered by | Reference / script |
    |----------|--------------|--------------------|
    | Resume / state read | every invocation | `scripts/state_writer.py read` |
    | Pre-flight install | first run on a fresh machine | `references/preflight/detect.md` *(Phase 2)* |
    | Intake (plain-English → brief) | "I want…", paragraph descriptions | `references/intake/question-bank.md` *(Phase 3)* |
    | Stack research | architect role | `references/playbooks/<app-type>.md` *(Phase 3 + Phase 7)* |
    | Scaffold dispatch | devops role | `scripts/scaffold_dispatch.py` *(Phase 3)* |
    | GSD handoff | every phase after scaffold | `references/roles/<role>.md` *(Phase 4)* |
    | Failure classification | every error | `scripts/failure_classifier.py` *(Phase 4)* |
    | Tutor mode / friendly errors | default UX | `references/ux/friendly-errors.md` *(Phase 5)* |
    | Ship to private GitHub | final phase | `references/ship/github-private.md` *(Phase 6)* |
    | Refuses (K8s, microservices, native mobile, public GitHub by default, Electron) | always | this section |
    ```

    **6. Cross-cutting rules** — short bullet list:

    ```markdown
    ## Cross-cutting Rules

    - **Privacy default:** Every GitHub repo OSBuilder creates is `--private`. Public requires explicit `--public` flag.
    - **Single-threaded:** Dev-team is narration only. Never spawn parallel sub-agents (DeepMind Dec 2025 documented 41-86.7% multi-agent failure rates).
    - **Reflection cap:** 3 retries per failure-signature (Aider's empirical limit). On cap → escalate via `/gsd-debug` → `/problem-solver` → structured user handoff.
    - **Always-scaffolder-first:** Never hand-write `package.json` / `tsconfig.json` / `pyproject.toml` when a scaffolder exists.
    - **Compose, don't fork:** If a sub-skill (gsd / brainiac / predator / code-tester / problem-solver / gsd-debug) lacks functionality, file an upstream issue. Never duplicate the skill into OSBuilder.
    - **Refuses by default (v1):** Kubernetes, microservices, service-mesh, Helm, Electron, native mobile, auto-deploy, public GitHub repos. Override via `--production-ready` or explicit flags.
    ```

    **7. Directory layout note** — point Claude at where things live:

    ```markdown
    ## Skill layout

    ```
    ~/.claude/skills/osbuilder/
    ├── SKILL.md              ← you are here (copied here by install.sh)
    ├── install.sh            ← idempotent local installer
    ├── references/           ← JIT-loaded by Claude (one level deep, per Anthropic)
    │   └── README.md         ← what each reference file owns (Phase 1 seed)
    ├── scripts/              ← executed (NOT loaded as context)
    │   ├── bootstrap.sh      ← POSIX shim — installs Python if missing
    │   ├── bootstrap.ps1     ← Windows shim — winget install Python.Python.3.13
    │   └── state_writer.py   ← 10-field state.md plumbing (stdlib-only)
    ├── assets/               ← templates copied verbatim into built apps (filled by Phase 6)
    └── examples/             ← gallery (filled by Phase 8)
    ```

    Per Anthropic's "no nested references" rule, references/ is one level deep. If sub-categories are needed, use filename prefixes (`web-next.md`, `web-vite.md`).
    ```

    **8. Footer**: a one-line note + version. Keep total under 200 lines.

    **CRITICAL constraints (every line is verified):**
    - Frontmatter `name` MUST equal `osbuilder` (not `osbuilder-v1`, not `OSBuilder`, not `os-builder`)
    - Frontmatter (the region between the opening `---` and closing `---`) MUST NOT contain `<` characters (XML rejection rule). All `<project-root>`, `<file>`, etc. placeholders go BELOW the closing `---`.
    - Description MUST NOT contain reserved words `anthropic` or `claude`
    - Description MUST be third-person ("Builds…", not "I build…" or "You build…")
    - Description MUST be ≤ 1024 chars (the example above is ~410 chars — well under)
    - SKILL.md MUST be ≤ 200 lines via `wc -l`
    - At least ONE `references/` link MUST appear in the body
    - The string `state_writer.py` MUST appear in the Resume Protocol section
    - The Resume Protocol command MUST reference `~/.claude/skills/osbuilder/scripts/state_writer.py` (installed path, not repo-root path — BLOCKER 2)
  </action>
  <verify>
    <automated>python3 -m pytest scripts/tests/test_skill_md.py -x -q && [ "$(awk '/^---$/{c++; next} c==1' SKILL.md | grep -c '<')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `SKILL.md` exists at repo root
    - First line is exactly `---` (frontmatter delimiter)
    - File contains line `name: osbuilder` (regex: `^name: osbuilder$`)
    - Closing `---` exists on its own line within the first 30 lines
    - `wc -l SKILL.md` reports a count ≤ 200
    - `grep -c "references/" SKILL.md` returns ≥ 1
    - `grep -E "## Resume Protocol|### Resume Protocol" SKILL.md` matches at least once
    - `grep "state_writer.py" SKILL.md` matches at least once (in the Resume Protocol section)
    - Resume Protocol section contains the EXACT installed path: `grep -q '~/.claude/skills/osbuilder/scripts/state_writer.py' SKILL.md` exits 0 (BLOCKER 2)
    - **Frontmatter `<` count is exactly zero (BLOCKER 2 / WARNING 2 acceptance gate):** `awk '/^---$/{c++; next} c==1' SKILL.md | grep -c "<"` returns exactly `0` (the awk extracts only the region between the first and second `---`; that region must contain zero `<` characters)
    - `python3 -m pytest scripts/tests/test_skill_md.py::test_frontmatter_valid -x` passes
    - `python3 -m pytest scripts/tests/test_skill_md.py::test_skill_md_line_count_under_200 -x` passes
    - `python3 -m pytest scripts/tests/test_skill_md.py::test_has_references_link -x` passes
  </acceptance_criteria>
  <done>
    SKILL.md committed at repo root; all 3 tests in `test_skill_md.py` pass; `wc -l SKILL.md` ≤ 200; awk-based frontmatter `<` check returns 0; Resume Protocol section present, post-frontmatter, and references the installed `~/.claude/skills/osbuilder/scripts/state_writer.py` path.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed references/README.md (resolves RESEARCH.md Assumption A10)</name>
  <files>references/README.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 925-928 "A10" — confirms a seed file is the recommended resolution; lines 942-947 "Open Question 2" — recommends shipping references/README.md describing what Phases 3-7 will add)
    - .planning/research/ARCHITECTURE.md (lines 96-141 "references/" tree — paste-source for what each future reference file owns)
    - SKILL.md (just-created — its routing table tells you which reference file paths must be documented as planned)
  </read_first>
  <action>
    Create `references/README.md` at `<repo-root>/references/README.md` with content explaining what each reference file in Phases 3-7 will own. This is documentation, not logic — it exists so that `wc -l references/` is non-zero and so a developer browsing the skill can see the progressive-disclosure plan. EXACT minimum content:

    ```markdown
    # OSBuilder references/

    JIT-loaded by Claude when the routing table in `../SKILL.md` matches a trigger. Per Anthropic's skill-authoring rules, this directory is **one level deep** — no nested subdirectories. If sub-categories are needed, use filename prefixes (`web-next.md`, `web-vite.md`).

    ## Files (current and planned)

    | File | Phase | Owns |
    |------|-------|------|
    | `README.md` | 1 (this file) | Index of what lives here |
    | `intake/question-bank.md` | 3 | Plain-English question library + "you decide" defaults |
    | `playbooks/web.md` | 3 | Next.js + Drizzle + Postgres-via-compose + Tailwind 4 + pnpm recipe |
    | `playbooks/ai-service.md` | 7 | FastAPI + uv + Pydantic v2 (uses `assets/fastapi-starter/`) |
    | `playbooks/cli.md` | 7 | Python + Typer + Rich + SQLite |
    | `playbooks/desktop.md` | 7 | Tauri 2 (refuses Electron) |
    | `playbooks/hub-platform.md` | 7 | Professor-Hub-style umbrella workspace |
    | `roles/pm.md`..`roles/tech-writer.md` | 4 + 5 | Per-role narration + delegation contract |
    | `preflight/{macos,linux,windows,detect}.md` | 2 | OS-specific install paths |
    | `failure-classifier/{matrix,retry-policy,escalation-handoff}.md` | 4 | Self-healing decision trees |
    | `ux/{question-bank,friendly-errors,tutor-narration}.md` | 5 | Common-person UX templates |
    | `ship/{github-private,runbook-template,env-template}.md` | 6 | Final delivery path |
    | `state-schema.md` | 4 | Authoritative state.md schema doc |

    ## Conventions

    - One file per concern. Files are ≤ 300 lines; if a file would exceed that, split via filename prefix.
    - Every file referenced in `SKILL.md` MUST exist in this directory once its phase ships. Until then, it appears in the routing table marked `*(Phase N)*`.
    - These files are loaded by Claude on-demand. They are NOT executed and not pre-loaded — only the routing table in SKILL.md is.

    ## Why this file exists in Phase 1

    Phase 1 Success Criterion #1 requires SKILL.md to "route via progressive disclosure to references/". Routing to a non-empty directory demonstrates the wiring is real even before downstream phases populate the per-recipe files. This file resolves Assumption A10 in `.planning/phases/01-foundation/01-RESEARCH.md`.
    ```

    Do NOT create any other files in `references/` — Phase 1 owns only this one seed.
  </action>
  <verify>
    <automated>test -f references/README.md && grep -q "JIT-loaded by Claude" references/README.md && grep -q "playbooks/web.md" references/README.md && grep -q "Phase 1" references/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `references/README.md` exists
    - File contains string `JIT-loaded` (anchors the progressive-disclosure intent)
    - File contains a markdown table row referencing `playbooks/web.md`
    - File mentions "Phase 1" (anchors why this seed exists)
    - File does NOT create any subdirectories under `references/` (verified by `find references -mindepth 2 -type d` returning nothing)
  </acceptance_criteria>
  <done>
    `references/README.md` committed; `find references/ -mindepth 2 -type d` returns empty (one-level-deep rule); SKILL.md's routing-table reference to `references/` resolves to a real, non-empty file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code ↔ SKILL.md frontmatter | Claude pre-loads frontmatter at session start; malformed frontmatter (XML tag, reserved word, oversized description) causes silent skill rejection (Pitfall 1) |
| SKILL.md ↔ references/ | Routing table contains future-phase references; Claude follows them on trigger; until those files exist, the routing rows are documentation only |
| repo-root SKILL.md ↔ ~/.claude/skills/osbuilder/SKILL.md | install.sh (Plan 03) COPIES (not symlinks) SKILL.md from repo root to install dir; Resume Protocol references the install location since end-users only see that |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-V5 | Tampering — frontmatter validation rejection | SKILL.md `name`, `description` | mitigate | Test `test_frontmatter_valid` enforces `name` regex (`^[a-z0-9-]{1,64}$`), `description` ≤ 1024 chars, no `<`, no "anthropic"/"claude" reserved words. RED test from Plan 01 turns GREEN here. Additional acceptance gate: `awk '/^---$/{c++; next} c==1' SKILL.md \| grep -c "<"` must equal 0 (frontmatter region only — angle brackets in body placeholders are fine). |
| T-1-V7 | Information Disclosure — Resume Protocol leaks raw error output | Resume Protocol section | mitigate | Resume Protocol instructs Claude to invoke `state_writer.py read` (which has its own clean-error contract from Plan 04) — SKILL.md never instructs Claude to print raw stack traces |
| T-1-Pitfall-2 | Bloat (denial-of-attention) — routing table grows past ~200 lines | SKILL.md routing table | mitigate | Routing table is categorical (~10 rows), not per-command. New playbooks add files in `references/`, not new SKILL.md rows. |
| T-1-Pitfall-10 | Repudiation — broken-looking links to future-phase files | SKILL.md routing table | accept (with mitigation) | "Build status" section documents which routes are real vs forward-looking; routes marked `*(Phase N)*`. Acceptable per RESEARCH.md A10 + Open Question 2 disposition. |
| T-1-BLOCKER-2 | Tampering — `<project-root>` placeholder leaks into frontmatter | SKILL.md frontmatter region | mitigate | Resume Protocol section is placed AFTER the closing `---` so `<project-root>` placeholders never enter the YAML description value. Acceptance gate via awk-extract + grep -c "<" = 0. |
</threat_model>

<verification>
1. `python3 -m pytest scripts/tests/test_skill_md.py -x -q` → exit 0 (all 3 tests pass)
2. `wc -l SKILL.md` → result ≤ 200
3. `grep -c "references/" SKILL.md` → ≥ 1
4. `grep -E "^## Resume Protocol|^### Resume Protocol" SKILL.md` → matches
5. `grep "state_writer.py" SKILL.md` → matches in Resume Protocol section
6. `grep -q '~/.claude/skills/osbuilder/scripts/state_writer.py' SKILL.md` → exit 0 (installed path used in Resume Protocol — BLOCKER 2)
7. `awk '/^---$/{c++; next} c==1' SKILL.md | grep -c "<"` → exactly `0` (frontmatter has no `<` — BLOCKER 2 / WARNING 2)
8. `find references -mindepth 2 -type d` → empty (one-level-deep enforced)
9. Frontmatter manual sanity: `head -30 SKILL.md` shows valid YAML between `---` delimiters
</verification>

<success_criteria>
- SKILL.md exists, is ≤ 200 lines, has valid Anthropic-spec frontmatter
- SKILL.md frontmatter region contains zero `<` characters (awk-extract acceptance gate)
- SKILL.md contains a routing table that references `references/`
- SKILL.md has a Resume Protocol section (post-frontmatter) that names the installed path `~/.claude/skills/osbuilder/scripts/state_writer.py`
- references/README.md exists and is one level deep
- All 3 tests in test_skill_md.py pass (RED → GREEN transition for Plan 01's stubs)
- Phase 1 Success Criteria #1 (frontmatter valid) and #5 (resume protocol exists) are satisfied
- FOUND-01 (skill installed with valid frontmatter) and FOUND-02 (≤200 lines, routes to references/) requirements are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Final SKILL.md line count (vs 200 cap)
- Frontmatter fields included (name, description, version, user-invocable, allowed-tools, argument-hint)
- Routing-table category count
- Test transition: which tests turned GREEN, which remain RED
- Frontmatter `<` count (must be 0 — BLOCKER 2 evidence)
- Resume Protocol path verification (must reference installed path — BLOCKER 2 evidence)
- Handoff: Plan 04 (state_writer.py) provides the implementation that Resume Protocol references; Plan 03 (install.sh) COPIES this SKILL.md and scripts/ into `~/.claude/skills/osbuilder/` so the Resume Protocol path becomes real on-disk
</output>
</output>
