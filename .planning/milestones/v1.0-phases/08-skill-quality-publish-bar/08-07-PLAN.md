---
phase: 08-skill-quality-publish-bar
plan: 07
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - assets/demo/osbuilder-demo.gif
  - assets/demo/osbuilder-demo.cast
  - assets/demo/RECORDING-CHECKLIST.md
autonomous: false
requirements: [QUAL-03]
must_haves:
  truths:
    - "assets/demo/osbuilder-demo.gif exists and auto-plays in GitHub READMEs (Pitfall 6: unedited end-to-end)"
    - "assets/demo/osbuilder-demo.cast exists as the asciinema source-of-truth (re-rendering possible)"
    - "Recording checklist documents the workflow + secret-redaction protocol so a future re-record is reproducible"
    - "GIF runtime <= 60 seconds (QUAL-03 success criterion 3)"
    - "Wave 0 stub test_readme.py::test_demo_asset_present flips RED → GREEN"
  artifacts:
    - path: "assets/demo/osbuilder-demo.gif"
      provides: "Auto-playing demo asset for README inline embed"
      max_size_mb: 5
    - path: "assets/demo/osbuilder-demo.cast"
      provides: "asciinema source for re-rendering / quality playback"
    - path: "assets/demo/RECORDING-CHECKLIST.md"
      provides: "Reproducible recording workflow + secret-redaction checklist"
      min_lines: 30
  key_links:
    - from: "README.md (08-06)"
      to: "assets/demo/osbuilder-demo.gif"
      via: "markdown image embed in `## 60-Second Demo` section"
      pattern: "!\\[.*\\]\\(assets/demo/osbuilder-demo\\.gif\\)"
    - from: "README.md (08-06)"
      to: "assets/demo/osbuilder-demo.cast"
      via: "markdown link in caption"
---

<objective>
Produce the 60-second demo asset (GIF + asciinema cast source) for README embed,
plus a recording checklist that codifies the secret-redaction + unedited-flow
protocol for future re-renders.

Purpose: QUAL-03 success criterion 3 demands "embed or link a 60-second demo
video that shows an end-to-end paragraph → working app on private GitHub flow."
GitHub strips `<script>` and most `<video>` configs; GIF is the lowest-friction
auto-play format (08-RESEARCH.md State of the Art table).

Output:
- assets/demo/osbuilder-demo.gif (auto-plays in README)
- assets/demo/osbuilder-demo.cast (asciinema source for re-rendering)
- assets/demo/RECORDING-CHECKLIST.md (reproducibility doc)

This plan is `autonomous: false` — Task 1 is automated (checklist + directory
prep) but Task 2 is a `checkpoint:human-action` (the actual recording requires
a human in front of a terminal capturing real keystrokes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md
@.planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md
@.planning/phases/08-skill-quality-publish-bar/08-HUMAN-UAT.md
@SKILL.md
@scripts/tests/test_readme.py

<interfaces>
<!-- No code interfaces. Asset specifications: -->
- GIF: `assets/demo/osbuilder-demo.gif`, <= 5MB, auto-plays in GitHub README inline
- asciinema cast: `assets/demo/osbuilder-demo.cast` — text format, source-of-truth
- Recording tools (verified 2026-05-02 against 08-RESEARCH.md):
  - asciinema (record .cast)
  - agg (asciinema -> GIF, replaces deprecated asciicast2gif)
  - Alternative: ScreenToGif (Windows), LICEcap (macOS), ffmpeg (cross-platform)
- 60s runtime hard cap (QUAL-03 SC-3)
- Pitfall 6: unedited end-to-end; Pitfall 2 (security): redact gh token, .env contents
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/demo/ directory + RECORDING-CHECKLIST.md</name>
  <files>
    assets/demo/RECORDING-CHECKLIST.md,
    assets/demo/.gitkeep
  </files>
  <action>
    Create `assets/demo/` directory and stamp the recording checklist + a `.gitkeep` so the directory exists in git even if Task 2 is deferred. The checklist codifies the unedited-end-to-end protocol (Pitfall 6) and the secret-redaction sequence (Pitfall 2 from 08-RESEARCH.md security section).

    `mkdir -p assets/demo`; create `assets/demo/.gitkeep` (empty).

    Create `assets/demo/RECORDING-CHECKLIST.md`:

    ```markdown
    # Demo Recording Checklist

    Reproducible workflow for recording the OSBuilder 60-second demo.
    Re-run this checklist any time the README's `assets/demo/osbuilder-demo.gif`
    needs to be re-rendered (e.g., after a stack version bump that changes
    visible terminal output).

    ## Goal

    A <= 60 second asset showing OSBuilder running paragraph → working app on
    private GitHub, end-to-end, with no cuts hiding friction.

    ## Pre-recording (security — Pitfall 2 from 08-RESEARCH.md)

    - [ ] Use a throwaway `gh auth login` profile, NOT your primary GitHub account
    - [ ] Run `history -c` (or close + reopen the terminal) to clear shell history
    - [ ] Confirm `printenv | grep -iE "(token|secret|password|key)"` returns empty
    - [ ] If the demo will use a real `.env`, redact it BEFORE recording — leave
          only `.env.example` content visible
    - [ ] Confirm `gh auth status` shows the throwaway account (not the real one)
    - [ ] Set terminal theme to a vanilla default (Terminal.app default on macOS,
          gnome-terminal default on Linux) — exotic themes render unpredictably in GIF

    ## Recording — option A: asciinema + agg (recommended)

    1. Install asciinema: `brew install asciinema` (macOS) or `pipx install asciinema` (Linux)
    2. Install agg: `cargo install --git https://github.com/asciinema/agg`
    3. Start recording: `asciinema rec assets/demo/osbuilder-demo.cast`
    4. Run the demo (see "Demo script" below)
    5. Exit recording: `Ctrl-D` or `exit`
    6. Convert to GIF: `agg --speed 2 assets/demo/osbuilder-demo.cast assets/demo/osbuilder-demo.gif`
       (`--speed 2` post-processes a 2-minute real run down to ~60s; capture every
       state visibly per Pitfall 6 — speed up but don't cut)

    ## Recording — option B: screen-recorder + ffmpeg fallback

    If asciinema is unavailable, capture via:
    - macOS: QuickTime → File → New Screen Recording → save .mov → `ffmpeg -i in.mov -vf "fps=10,scale=800:-1" assets/demo/osbuilder-demo.gif`
    - Linux: SimpleScreenRecorder → save .mp4 → `ffmpeg -i in.mp4 -vf "fps=10,scale=800:-1" assets/demo/osbuilder-demo.gif`
    - Windows: ScreenToGif → save .gif directly to assets/demo/osbuilder-demo.gif

    Note: option B does NOT produce an asciinema `.cast` source. Document that in
    `assets/demo/RECORDING-CHECKLIST.md` post-recording so future maintainers know
    the cast file is not in sync with the GIF.

    ## Demo script (the 60s itself)

    Pick a single, simple intake — TODO web app is the canonical example
    (matches Phase 3 SC-1 verbatim). Run from a clean working directory.

    1. Type: `/osbuilder I want a TODO web app where I can add tasks and check them off`
    2. Show OSBuilder picking the web playbook (1 line of narration)
    3. Show preflight check (1-2 lines: "all prerequisites already installed")
    4. Show scaffolder running: `pnpm create next-app...` (compressed via --speed)
    5. Show GSD plan-phase emitting plan files (1-2 lines)
    6. Show execute-phase landing the first phase (compressed)
    7. Show verify pass (1 line: "✓ TODO list renders, ✓ check-off works")
    8. Show `gh repo create --private` + final URL printed
    9. End frame: a real GitHub repo URL visible (private — that's expected)

    Total runtime AFTER `--speed` post-processing: <= 60 seconds.

    ## Post-recording sanity checks

    - [ ] `ffprobe assets/demo/osbuilder-demo.gif 2>&1 | grep Duration` (or replay) confirms <= 60s
    - [ ] `du -sh assets/demo/osbuilder-demo.gif` reports <= 5MB (GitHub's recommended GIF cap)
    - [ ] Replay the GIF and confirm: NO `gh auth token` visible, NO `.env` contents visible, NO ssh key fragments visible
    - [ ] If GIF was produced via asciinema, also commit `osbuilder-demo.cast` (the source)
    - [ ] Replay end frame: a real GitHub repo URL is visible (proof of end-to-end)
    - [ ] Run `uv run pytest scripts/tests/test_readme.py::test_demo_asset_present` — should pass

    ## When the GIF needs re-rendering

    Triggers:
    - SKILL.md narration text changes (terminal output looks different)
    - Stack defaults change (Next.js 17 lands; demo still shows 16)
    - A new playbook becomes the demo subject (TODO web → AI summarizer)

    Re-run this entire checklist. The asciinema `.cast` source is preserved so
    you can replay the original at any time via `asciinema play
    assets/demo/osbuilder-demo.cast`.

    ## See also

    - `.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md` Pitfall 2 (security), Pitfall 6 (unedited honesty)
    - `.planning/phases/08-skill-quality-publish-bar/08-HUMAN-UAT.md` row 2 (manual gate for honesty review)
    - README.md `## 60-Second Demo` section (where the GIF embeds)
    ```

    Do NOT commit a placeholder GIF — assets/demo/.gitkeep is enough to keep the dir tracked. Task 2's checkpoint produces the actual binary.
  </action>
  <read_first>
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Pitfall 2 security; Pitfall 6 honesty; State of the Art asciinema/agg)
    - .planning/phases/08-skill-quality-publish-bar/08-HUMAN-UAT.md (row 2 — manual UAT criteria for the demo)
    - SKILL.md (intake examples — line 36 has the canonical paragraph format)
  </read_first>
  <acceptance_criteria>
    - Directory `assets/demo/` exists
    - File `assets/demo/.gitkeep` exists (empty file is fine)
    - File `assets/demo/RECORDING-CHECKLIST.md` exists with line count >= 30
    - Checklist contains both Pitfall 2 (security) and Pitfall 6 (honesty) references
    - Checklist contains both option A (asciinema+agg) and option B (screen-recorder+ffmpeg) recording paths
    - Checklist contains the post-recording size cap (<= 5MB) and runtime cap (<= 60s) checks
  </acceptance_criteria>
  <verify>
    <automated>test -d assets/demo && test -f assets/demo/.gitkeep && test -f assets/demo/RECORDING-CHECKLIST.md && wc -l assets/demo/RECORDING-CHECKLIST.md | awk '{exit ($1 >= 30) ? 0 : 1}' && grep -q "Pitfall 2" assets/demo/RECORDING-CHECKLIST.md && grep -q "Pitfall 6" assets/demo/RECORDING-CHECKLIST.md && grep -q "asciinema" assets/demo/RECORDING-CHECKLIST.md && grep -q "60s\|60 second" assets/demo/RECORDING-CHECKLIST.md</automated>
  </verify>
  <done>
    assets/demo/ directory exists with .gitkeep; RECORDING-CHECKLIST.md committed with both recording paths and security/honesty protocol.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [HUMAN]: Record the 60-second demo and commit the assets</name>
  <what-built>
    The recording checklist + directory scaffold (Task 1). All automation that Claude can do is done.
  </what-built>
  <how-to-verify>
    Open `assets/demo/RECORDING-CHECKLIST.md` and follow it end-to-end:

    1. **Pre-recording security pass.** Throwaway gh login, history clear, redact .env, vanilla terminal theme.
    2. **Record via asciinema (preferred) or screen-recorder (fallback).** See checklist Options A and B for exact commands.
    3. **Run the documented demo script.** Single intake (TODO web app), show every state — no cuts hiding friction.
    4. **Convert to GIF (option A): `agg --speed 2 assets/demo/osbuilder-demo.cast assets/demo/osbuilder-demo.gif`.**
    5. **Post-recording sanity checks (in checklist):**
       - File size: `du -sh assets/demo/osbuilder-demo.gif` reports <= 5MB
       - Runtime: replay the GIF; visible runtime <= 60 seconds
       - No secrets visible: gh tokens, .env contents, ssh keys all redacted/absent
       - Final frame: a real GitHub repo URL is visible (proof of end-to-end)
    6. **Commit the assets:**
       ```bash
       git add assets/demo/osbuilder-demo.gif assets/demo/osbuilder-demo.cast
       git commit -m "feat(08): record 60s demo asset (QUAL-03)"
       ```
    7. **Confirm the test passes:** `uv run pytest scripts/tests/test_readme.py::test_demo_asset_present -x` reports 1 passed.
    8. **Confirm 08-HUMAN-UAT.md row 2 can now be marked.** Update its `result:` line to `passed` (or `failed: <description>` if the recording revealed a real OSBuilder issue worth a separate gap-closure).

    If the GIF turned out > 60s or > 5MB, re-run with shorter intake (e.g., compress phase 6 narration) or higher `--speed` factor. Document any deviations in the SUMMARY.

    If you cannot record today (no time, no clean environment), reply with `defer` and mark this plan's `done: deferred — recording rescheduled to <date>`. The phase ships with `test_demo_asset_present` SKIPPING (acceptable for the GREEN suite per the file-existence skip-guard pattern), and `08-HUMAN-UAT.md` row 2 stays `pending`. README references the asset path even though the file isn't yet committed — broken-link-in-README is a known accepted state until recording happens.
  </how-to-verify>
  <resume-signal>
    Reply with `recorded` (assets committed; tests pass) OR `defer` (recording rescheduled — describe blocker and target date).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recording terminal → committed GIF | Anything visible in the recording shell becomes public when the repo publishes |
| recording terminal → asciinema .cast | Same — .cast is plaintext containing every visible character |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Information Disclosure | Recording captures `gh auth token` or .env values | mitigate | RECORDING-CHECKLIST.md pre-recording section enforces throwaway gh login + history clear + .env redaction; post-recording replay sanity check confirms no secrets visible |
| T-08-22 | Information Disclosure | Recording captures the user's real ~/ path with username | accept | Username in shell prompt is low-risk for an open-source skill author (will be public anyway via git commit metadata) |
| T-08-23 | Repudiation | Demo shows aspirational UX, not real behavior | mitigate | Pitfall 6 mitigation: checklist enforces unedited end-to-end + 08-HUMAN-UAT.md row 2 is the manual honesty gate |
| T-08-24 | Tampering | A future maintainer re-renders the GIF via screen-recorder fallback and the .cast source goes stale | mitigate | RECORDING-CHECKLIST.md "When the GIF needs re-rendering" section instructs maintainers to either (a) use asciinema-based re-render to keep .cast in sync OR (b) document the desync explicitly |
</threat_model>

<verification>
- assets/demo/ exists with checklist
- After Task 2 completes (or is deferred): osbuilder-demo.gif + .cast either both present (recorded path) or both absent (deferred path), with deferred status documented in SUMMARY
- 08-HUMAN-UAT.md row 2 has its `result:` updated to `passed` (or `pending` if deferred)
</verification>

<success_criteria>
- QUAL-03 success criterion 3: "60-second demo video that shows an end-to-end paragraph → working app on private GitHub flow" — asset committed (or deferred with documented unblocker)
- Pitfall 6 mitigation: unedited end-to-end protocol codified in checklist
- Pitfall 2 mitigation: secret-redaction protocol codified in checklist
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-quality-publish-bar/08-07-SUMMARY.md` documenting:
- Whether assets were recorded (Task 2 = `recorded`) or deferred (Task 2 = `defer`)
- If recorded: GIF size, runtime, asciinema or screen-recorder path used, sanity-check confirmations
- If deferred: blocker, target re-record date, downstream UAT row impact
- 08-HUMAN-UAT.md row 2 status update
</output>
