---
phase: 08-skill-quality-publish-bar
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-03]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - ".github/workflows/ci.yml exists and is committed"
    - "CI workflow runs on every PR to main and every push to main"
    - "lint-skill-md job invokes scripts/check_skill_md_length.py before the test job"
    - "test job runs `uv run pytest` against the full suite"
    - "All actions pinned to exact major (@v6) — no @latest (Security V14)"
    - "Wave 0 stubs in scripts/tests/test_ci_workflow.py flip RED → GREEN (3/3 tests passing)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "QUAL-01 CI surface — fails PRs that push SKILL.md > 200 lines"
      contains: "scripts/check_skill_md_length.py"
      min_lines: 25
  key_links:
    - from: ".github/workflows/ci.yml lint-skill-md job"
      to: "scripts/check_skill_md_length.py (08-03)"
      via: "run: python3 scripts/check_skill_md_length.py"
      pattern: "scripts/check_skill_md_length\\.py"
    - from: ".github/workflows/ci.yml test job"
      to: "scripts/tests/ pytest suite"
      via: "uv run pytest"
      pattern: "uv run pytest"
---

<objective>
Create the CI surface that enforces QUAL-01 on every PR. Eats OSBuilder's own
dog food — uses the same `assets/ci-workflows/python.yml.tmpl` template OSBuilder
emits for built repos (08-PATTERNS.md "exact" match).

Purpose: A PR pushing SKILL.md to 201 lines must fail CI before review. Combined
with the existing in-process pytest assertion, this gives two surfaces enforcing
the same invariant.

Output:
- .github/workflows/ci.yml (~30 lines, two jobs: lint-skill-md + test)
- Wave 0 stubs in test_ci_workflow.py flip to GREEN (3/3 tests passing)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md
@.planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md
@assets/ci-workflows/python.yml.tmpl
@scripts/tests/test_ci_workflow.py
@scripts/check_skill_md_length.py

<interfaces>
<!-- Pinned action versions verified 2026-05-02 against GitHub Marketplace -->
- actions/checkout@v6 (latest)
- actions/setup-python@v6 (Python 3.13 supported)
- astral-sh/setup-uv@v6 (mature --frozen + cache)
- runs-on: ubuntu-latest

Test contract (test_ci_workflow.py from 08-01):
- File path: .github/workflows/ci.yml exists
- Must NOT contain "@latest" anywhere
- Must contain "actions/checkout@v6"
- Must contain "actions/setup-python@v6"
- Must contain "scripts/check_skill_md_length.py"

Source template (assets/ci-workflows/python.yml.tmpl, 23 lines verbatim — copy + extend):
```yaml
# OSBuilder default — Python + uv CI workflow (SCL-04)
name: Test on PR
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --frozen
      - run: uv run pytest
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create the CI workflow at `.github/workflows/ci.yml`. Follow 08-PATTERNS.md "Extension for Phase 8" verbatim — two jobs (lint-skill-md + test), test depends on lint via `needs:`. Run on PR-to-main AND push-to-main.

    Note: `mkdir -p .github/workflows` first; this is the first GitHub Actions workflow in the repo (the existing `.tmpl` file is in `assets/`, not `.github/`).

    Full file content:

    ```yaml
    # OSBuilder CI — QUAL-01 line-count gate + full pytest suite
    # Source: assets/ci-workflows/python.yml.tmpl (extended with lint-skill-md job)
    # Verified 2026-05-02: actions/checkout@v6, actions/setup-python@v6, astral-sh/setup-uv@v6
    name: CI

    on:
      pull_request:
        branches: [main]
      push:
        branches: [main]

    jobs:
      lint-skill-md:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v6
          - uses: actions/setup-python@v6
            with:
              python-version: '3.13'
          - name: Lint SKILL.md line count (QUAL-01)
            run: python3 scripts/check_skill_md_length.py

      test:
        runs-on: ubuntu-latest
        needs: lint-skill-md
        steps:
          - uses: actions/checkout@v6
          - uses: actions/setup-python@v6
            with:
              python-version: '3.13'
          - uses: astral-sh/setup-uv@v6
          - run: uv sync --frozen
          - run: uv run pytest
    ```

    **Constraints:**
    - Do NOT use `@latest` anywhere — Security V14 requires exact major pins
    - Do NOT add other jobs (no Black, no ruff format, no mypy) — Phase 8 scope is QUAL-01 only; expansion comes later
    - The `needs: lint-skill-md` ordering means a SKILL.md > 200 lines fails the PR before pytest even runs (faster feedback)
    - Single trigger: PR to main + push to main. Do NOT add `workflow_dispatch`, `schedule`, or other triggers — out of scope
  </action>
  <read_first>
    - assets/ci-workflows/python.yml.tmpl (verbatim source template)
    - scripts/tests/test_ci_workflow.py (test contract — what shapes the tests grep for)
    - .planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md (.github/workflows/ci.yml section, lines 506-567)
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Validation Architecture row for CI workflow + Pattern 2)
    - scripts/check_skill_md_length.py (confirm path; CI must invoke this exact path)
  </read_first>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists
    - File line count >= 25 (`wc -l .github/workflows/ci.yml` reports >= 25)
    - File contains two job names: `lint-skill-md:` and `test:` (both at column 2)
    - File contains `needs: lint-skill-md` (test job depends on lint)
    - File contains `actions/checkout@v6`
    - File contains `actions/setup-python@v6`
    - File contains `astral-sh/setup-uv@v6`
    - File does NOT contain `@latest` (`! grep "@latest" .github/workflows/ci.yml`)
    - File contains the literal `scripts/check_skill_md_length.py`
    - File contains the literal `uv run pytest`
    - File contains both `pull_request:` AND `push:` triggers
    - Wave 0 tests pass: `uv run pytest scripts/tests/test_ci_workflow.py -x` reports 3 passed (no skips)
    - YAML is well-formed: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>&1 | grep -v "ModuleNotFoundError"` succeeds OR (yaml not in stdlib) `python3 -c "import sys; lines = open('.github/workflows/ci.yml').read().splitlines(); assert all(not l.startswith('\t') for l in lines), 'no tabs allowed in YAML indent'"`
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/ci.yml && uv run pytest scripts/tests/test_ci_workflow.py -x -v && ! grep -q "@latest" .github/workflows/ci.yml && grep -q "actions/checkout@v6" .github/workflows/ci.yml && grep -q "actions/setup-python@v6" .github/workflows/ci.yml && grep -q "astral-sh/setup-uv@v6" .github/workflows/ci.yml && grep -q "scripts/check_skill_md_length.py" .github/workflows/ci.yml && grep -q "needs: lint-skill-md" .github/workflows/ci.yml && python3 -c "lines = open('.github/workflows/ci.yml').read().splitlines(); assert all(not l.startswith(chr(9)) for l in lines), 'tabs in YAML indent'"</automated>
  </verify>
  <done>
    .github/workflows/ci.yml exists with two jobs (lint-skill-md depends-on test), all actions pinned to @v6, invokes scripts/check_skill_md_length.py before pytest. 3/3 Wave 0 tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo | Runs untrusted PR code; CI must not execute workflow modifications from PR-changed workflow files (GitHub default for forks already enforces this) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Supply chain | actions/checkout@latest could pull a compromised release | mitigate | Pinned to @v6 (exact major); upgrade cadence documented in references/version-policy.md (Task in 08-02) |
| T-08-16 | Tampering | Malicious PR modifies ci.yml to skip lint job | accept | Standard GitHub Actions behavior — PR-based workflow changes run with restricted GITHUB_TOKEN; main-branch protection (out of scope for this plan) is the long-term mitigation |
| T-08-17 | Information Disclosure | CI logs expose secrets via `printenv` | accept | No secrets are referenced in this workflow (no `env:` block, no `secrets.X`); future expansion would need explicit guards |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists with both jobs and explicit `needs:` dependency
- All action versions pinned to @v6
- 3/3 Wave 0 tests in test_ci_workflow.py GREEN
- YAML uses 2-space indent only (no tabs)
</verification>

<success_criteria>
- QUAL-01 success criterion 1: "Lint script reports SKILL.md line count <= 200 in CI on every PR; PR pushing SKILL.md to 201 lines fails the check" — surface exists; manual UAT will verify by pushing a synthetic >200-line PR (08-HUMAN-UAT.md row 1 is the natural place to add this once a remote exists, or it can be tested locally via `act` if the developer wants offline confirmation)
- Eats own dog food: workflow shape mirrors what OSBuilder generates for built repos
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-quality-publish-bar/08-05-SUMMARY.md` documenting:
- Final ci.yml line count
- Pytest results (3/3 passing)
- Note on whether developer ran `act` locally to confirm workflow validity (optional)
- Confirmation that grep for `@latest` returns nothing
</output>
