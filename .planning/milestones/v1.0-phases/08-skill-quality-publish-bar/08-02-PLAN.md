---
phase: 08-skill-quality-publish-bar
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - SKILL.md
  - references/version-policy.md
autonomous: true
requirements: [QUAL-05]
must_haves:
  truths:
    - "SKILL.md frontmatter declares minimum versions for all 5 sub-skills via `requires:` block"
    - "SKILL.md remains <= 200 lines after edit (Pitfall 1 — currently 130, leaving 60+ headroom)"
    - "references/version-policy.md documents the OSBuilder-local `requires:` convention with format, behavior, and Anthropic-future-proofing disclaimer"
    - "Existing test_skill_md.py tests stay green (frontmatter still valid)"
  artifacts:
    - path: "SKILL.md"
      provides: "Source of truth for sub-skill version minimums"
      contains: "requires:"
    - path: "references/version-policy.md"
      provides: "Policy doc explaining the requires: convention"
      min_lines: 30
  key_links:
    - from: "SKILL.md frontmatter"
      to: "scripts/check_skill_versions.py (08-03)"
      via: "_read_frontmatter() reads requires: block as nested dict"
      pattern: "requires:\\n(  [a-z-]+: [0-9.]+\\n)+"
    - from: "references/version-policy.md"
      to: "scripts/check_skill_versions.py (08-03)"
      via: "policy doc cross-referenced in script docstring; behavior table matches script's three-mode logic"
---

<objective>
Add `requires:` block to SKILL.md frontmatter declaring sub-skill minimum versions;
create `references/version-policy.md` documenting the OSBuilder-local convention.

Purpose: QUAL-05 prerequisite. The validator script (08-03) reads this block as
the single source of truth for minimums. Data-driven > hardcoded constants
(08-RESEARCH.md "Don't Hand-Roll" + Pitfall 7).

Output:
- SKILL.md gains 5-line `requires:` block in frontmatter (still <= 200 lines)
- references/version-policy.md created (~50 lines) with format, behavior table, See also footer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md
@.planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md
@SKILL.md
@references/refuse-list.md
@references/markers.md
@scripts/tests/test_skill_md.py

<interfaces>
<!-- Verified frontmatter constraints from existing test_skill_md.py + Anthropic spec -->
- name regex: ^[a-z0-9-]{1,64}$
- description max chars: 1024
- description must NOT contain `<` (XML-like tag forbidden)
- frontmatter delimited by `---\n...\n---\n` (re.DOTALL)
- Hand-parser supports nested keys under `requires:` via 2-space indent (08-PATTERNS.md _read_frontmatter excerpt)

Verified sub-skill version state (2026-05-02, direct read of ~/.claude/skills/<name>/SKILL.md):
- gsd: NO version field
- brainiac: version: 6.0.0
- predator: NO version field
- code-tester: version: 3.1.0
- problem-solver: version: 3.0.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requires: block to SKILL.md frontmatter</name>
  <files>SKILL.md</files>
  <action>
    Edit SKILL.md frontmatter (lines 1-8) to add a `requires:` block AFTER `argument-hint:` and BEFORE the closing `---`. Use exact 2-space indent for nested sub-keys (parser depends on this).

    **Before** (current SKILL.md lines 1-8 — verbatim):
    ```yaml
    ---
    name: osbuilder
    description: >
      Builds end-to-end applications from a plain-English description. Orchestrates a virtual dev team (PM, Architect, Frontend, Backend, DevOps, QA, Reviewer, Tech Writer) over the user's existing skill ecosystem (gsd, brainiac, predator, code-tester, problem-solver) to ship a working app to a private GitHub repo. Common-person friendly with tutor mode on by default. Triggers on. /osbuilder, build me an app, build a website, build a tool, build like Professor Hub.
    allowed-tools: Read, Write, Edit, Bash, Agent, Glob, Grep, WebSearch, WebFetch
    user-invocable: true
    argument-hint: "[brief or @path/to/spec.md or 'build like ./reference-app']"
    ---
    ```

    **After** (add 6 lines for the requires: block):
    ```yaml
    ---
    name: osbuilder
    description: >
      Builds end-to-end applications from a plain-English description. Orchestrates a virtual dev team (PM, Architect, Frontend, Backend, DevOps, QA, Reviewer, Tech Writer) over the user's existing skill ecosystem (gsd, brainiac, predator, code-tester, problem-solver) to ship a working app to a private GitHub repo. Common-person friendly with tutor mode on by default. Triggers on. /osbuilder, build me an app, build a website, build a tool, build like Professor Hub.
    allowed-tools: Read, Write, Edit, Bash, Agent, Glob, Grep, WebSearch, WebFetch
    user-invocable: true
    argument-hint: "[brief or @path/to/spec.md or 'build like ./reference-app']"
    requires:
      gsd: 1.0.0
      brainiac: 6.0.0
      predator: 1.0.0
      code-tester: 3.1.0
      problem-solver: 3.0.0
    ---
    ```

    **Version values rationale (verified 2026-05-02):**
    - `brainiac: 6.0.0` — installed version 6.0.0 (verified)
    - `code-tester: 3.1.0` — installed version 3.1.0 (verified)
    - `problem-solver: 3.0.0` — installed version 3.0.0 (verified)
    - `gsd: 1.0.0` — gsd has no version field installed; 1.0.0 is the OSBuilder-side baseline. The validator (08-03) treats missing version as warn-not-block, so this baseline is documentation for future versions of gsd that DO declare a version.
    - `predator: 1.0.0` — predator has no version field installed; same rationale as gsd.

    **CRITICAL constraints (Pitfall 1):**
    - Do NOT modify body content (lines 9+).
    - Do NOT add explanatory comments or callouts in the body — that policy text lives in `references/version-policy.md` (Task 2).
    - SKILL.md MUST remain `<= 200` lines after this edit. Current = 130 lines; +6 lines = 136 lines. Headroom preserved.
    - The `requires:` value (after the colon) MUST be empty/whitespace-only — sub-keys go on the next line indented exactly 2 spaces (frontmatter parser in test_skill_md.py:26-39 + extended parser in 08-PATTERNS.md depends on this).
  </action>
  <read_first>
    - SKILL.md (entire file, to confirm 130 lines and frontmatter shape)
    - scripts/tests/test_skill_md.py (NAME_REGEX, DESCRIPTION_MAX_CHARS, hand-parser at lines 15-40)
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Pitfall 1 line-count headroom; Pitfall 7 namespace decision)
    - .planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md (SKILL.md modification section, lines 764-798)
  </read_first>
  <acceptance_criteria>
    - `wc -l SKILL.md` reports a value between 131 and 140 (was 130, +6 expected)
    - `wc -l SKILL.md` reports `<= 200` (QUAL-01 invariant)
    - `grep -c "^requires:" SKILL.md` returns exactly 1
    - `grep -E "^  (gsd|brainiac|predator|code-tester|problem-solver): [0-9.]+$" SKILL.md` returns exactly 5 lines
    - `grep -c "^---$" SKILL.md` returns exactly 2 (frontmatter delimiters unchanged in count)
    - Pre-existing tests still GREEN: `uv run pytest scripts/tests/test_skill_md.py -x` passes
    - Body content (line 9 onwards) byte-identical to before — `head -n 8 SKILL.md > /tmp/before_fm.txt` then verify lines 9+ unchanged
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest scripts/tests/test_skill_md.py -x && wc -l SKILL.md | awk '{exit ($1 >= 131 && $1 <= 200) ? 0 : 1}' && grep -c "^requires:" SKILL.md | awk '$1 == 1 {exit 0} {exit 1}' && [ "$(grep -cE "^  (gsd|brainiac|predator|code-tester|problem-solver): [0-9.]+$" SKILL.md)" = "5" ]</automated>
  </verify>
  <done>
    SKILL.md frontmatter contains the 6-line requires: block; line count between 131-140; existing test_skill_md.py tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create references/version-policy.md</name>
  <files>references/version-policy.md</files>
  <action>
    Create `references/version-policy.md` following the analog structure of `references/refuse-list.md` (cross-reference header + policy table + See also footer). 08-PATTERNS.md "references/version-policy.md" section provides the verbatim template — use it.

    Content (copy verbatim, do not editorialize):

    ```markdown
    # OSBuilder Version Policy (`requires:` convention)

    > Cross-referenced by `scripts/check_skill_versions.py:check_versions`. The
    > `requires:` block in OSBuilder's SKILL.md frontmatter is the source of truth
    > for sub-skill minimum versions.
    >
    > **NOT a standard Anthropic frontmatter field** (verified 2026-05-02 against
    > `code.claude.com/docs/en/skills` and
    > `platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices`).
    > This is an OSBuilder-local convention; future Anthropic spec changes are
    > tracked in Pitfall 7 of `.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md`.

    ## Format

    In SKILL.md frontmatter:

    ```yaml
    requires:
      gsd: 1.0.0
      brainiac: 6.0.0
      predator: 1.0.0
      code-tester: 3.1.0
      problem-solver: 3.0.0
    ```

    Indentation is exactly 2 spaces. Versions are dotted integers
    (`MAJOR.MINOR.PATCH`); pre-releases (`1.0.0-beta1`) are not supported in v1.

    ## Behavior on first invocation

    `scripts/check_skill_versions.py` runs once per Claude Code session, gated by
    the `~/.osbuilder/last_check.txt` marker (presence = already checked this
    session). For each sub-skill listed in `requires:`:

    | Installed state | Behavior | Exit code |
    |-----------------|----------|-----------|
    | Sub-skill `version:` >= declared minimum | Pass silently | 0 |
    | Sub-skill `version:` < declared minimum | **BLOCK** with friendly upgrade command | 1 |
    | Sub-skill `version:` field absent (gsd, predator as of 2026-05-02) | **WARN** but proceed (non-blocking) | 0 |
    | Sub-skill not installed at `~/.claude/skills/<name>/` | **BLOCK** with install command | 1 |

    Rationale for warn-not-block on missing version: of the 5 sub-skills, gsd and
    predator currently expose no `version:` frontmatter field. Blocking on absence
    would brick first-run for every OSBuilder user. The composition rule says:
    fix the sub-skill, never fork it. We file issues against gsd & predator to add
    `version:` blocks; in the meantime, warn-and-proceed is the user-friendly
    posture.

    ## Resetting the marker

    To force a re-check (e.g., after upgrading a sub-skill mid-session):

    ```bash
    rm -f ~/.osbuilder/last_check.txt
    ```

    Next `/osbuilder` invocation will re-run the validator.

    ## Why per-user-global, not per-project

    The marker file lives at `~/.osbuilder/last_check.txt`, not under any project's
    `.planning/`. Version drift is a per-machine concern (you upgrade brainiac on
    your laptop, not on a per-project basis). See Pitfall 3 in 08-RESEARCH.md for
    the full rationale.

    ## Updating the minimums

    1. Bump the version value in SKILL.md `requires:` block.
    2. Confirm the new minimum is realistic by reading
       `~/.claude/skills/<name>/SKILL.md` for the currently-installed version.
    3. The validator picks up the change on next session (or after marker reset).

    ## See also

    - `SKILL.md` — frontmatter `requires:` block (source of truth)
    - `scripts/check_skill_versions.py` — implementation
    - `~/.claude/skills/{gsd,brainiac,predator,code-tester,problem-solver}/SKILL.md` — installed versions
    - `.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md` Pitfall 2 (missing-version policy), Pitfall 7 (Anthropic-future-proofing)
    ```

    File is documentation-only (not loaded at runtime). The validator reads SKILL.md frontmatter directly; this file documents intent for human maintainers.
  </action>
  <read_first>
    - references/refuse-list.md (analog: cross-reference header + policy table + See also footer)
    - references/markers.md (analog: maintainer-reference doc shape)
    - .planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md (references/version-policy.md section, lines 713-760)
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Pitfalls 2, 3, 7)
  </read_first>
  <acceptance_criteria>
    - File `references/version-policy.md` exists
    - File line count >= 30 (`wc -l references/version-policy.md` reports >= 30)
    - File contains `# OSBuilder Version Policy` heading
    - File contains `## Format` section with a yaml fenced block showing `requires:`
    - File contains `## Behavior on first invocation` section with a 4-row markdown table (`| Installed state |`)
    - File contains `## See also` footer with cross-references
    - Grep `grep -c "^| " references/version-policy.md` returns >= 5 (table header + 4 data rows minimum)
    - Grep `grep "scripts/check_skill_versions.py" references/version-policy.md` returns >= 1 cross-link
  </acceptance_criteria>
  <verify>
    <automated>test -f references/version-policy.md && wc -l references/version-policy.md | awk '{exit ($1 >= 30) ? 0 : 1}' && grep -q "^# OSBuilder Version Policy" references/version-policy.md && grep -q "^## Format" references/version-policy.md && grep -q "^## Behavior on first invocation" references/version-policy.md && grep -q "scripts/check_skill_versions.py" references/version-policy.md</automated>
  </verify>
  <done>
    references/version-policy.md exists, >= 30 lines, contains required sections + cross-links to script and SKILL.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static doc → SKILL.md frontmatter | Hand-rolled YAML parser must handle the new `requires:` block without breaking existing tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | SKILL.md frontmatter parser breaks on the new block | mitigate | Acceptance criteria: existing test_skill_md.py tests still pass; parser pattern from 08-PATTERNS.md verified against the analog |
| T-08-05 | Information Disclosure | requires: block contains shell metacharacters | mitigate | Pinned values are dotted-integer literals; validator (08-03) re-applies regex `^[0-9.]{1,16}$` allowlist |
| T-08-06 | Repudiation | Future Anthropic spec adds conflicting `requires:` semantics | accept | Documented disclaimer in references/version-policy.md; migration path is renaming to `osbuilder-requires:` if conflict materializes (Pitfall 7) |
</threat_model>

<verification>
- SKILL.md frontmatter contains 5 sub-skill version minimums
- SKILL.md line count: 131-140 (was 130; +6 expected for the requires: block)
- references/version-policy.md exists with 4-mode behavior table and See also footer
- `uv run pytest scripts/tests/test_skill_md.py` GREEN (frontmatter still valid)
</verification>

<success_criteria>
- `requires:` block parsable by the extended hand-parser pattern from 08-PATTERNS.md
- Version values match installed-version reality for the 3 sub-skills that DO declare version
- Policy doc cross-links to the implementation script (08-03 will land it)
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-quality-publish-bar/08-02-SUMMARY.md` documenting:
- Final SKILL.md line count
- requires: block contents (verbatim)
- references/version-policy.md line count
- Confirmation of test_skill_md.py still GREEN
</output>
