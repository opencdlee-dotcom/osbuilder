---
phase: 08-skill-quality-publish-bar
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-04, 08-07, 08-08]
files_modified:
  - README.md
autonomous: true
requirements: [QUAL-02, QUAL-03]
must_haves:
  truths:
    - "README.md exists at repo root (currently does not exist)"
    - "README contains the install one-liner (`curl -fsSL ... | sh`) using the URL locked in 08-URL-LOCK.md"
    - "README contains the audited-install alternative (git clone + ./install.sh) for security-conscious users"
    - "README has a `## How OSBuilder Works` section mapping all 8 dev-team roles to plain-English narration"
    - "README links the demo asset at assets/demo/osbuilder-demo.gif (or .cast)"
    - "README documents the `--production-ready` flag with all 7 named upgrades from production_phase_writer.NAMED_UPGRADES"
    - "Wave 0 stubs in scripts/tests/test_readme.py flip RED → GREEN (5/5 tests passing)"
  artifacts:
    - path: "README.md"
      provides: "Open-source publish-bar README — install + dev-team metaphor + demo + production-ready doc"
      min_lines: 80
  key_links:
    - from: "README.md install section"
      to: ".planning/phases/08-skill-quality-publish-bar/08-URL-LOCK.md RAW_INSTALL_URL"
      via: "URL substituted into the curl one-liner"
      pattern: "curl -fsSL .*install\\.sh.*\\| sh"
    - from: "README.md demo section"
      to: "assets/demo/osbuilder-demo.gif (08-07)"
      via: "markdown image embed"
      pattern: "!\\[.*\\]\\(assets/demo/osbuilder-demo\\.gif\\)"
    - from: "README.md --production-ready section"
      to: "scripts/production_phase_writer.NAMED_UPGRADES (Phase 6)"
      via: "documents all 7 upgrade names"
      pattern: "(observability|migrations|healthchecks|secret-manager|sentry|rate-limiting|backups)"
---

<objective>
Create the repo-root README.md from scratch (currently does not exist). It is the
shop-window for OSBuilder: install one-liner, dev-team metaphor explainer, embedded
60-second demo, and `--production-ready` flag documentation.

Purpose: QUAL-02 (install one-liner) + QUAL-03 (dev-team metaphor + demo) +
SC-6 (`--production-ready` doc — Phase 6 already shipped the implementation;
this is the verification + documentation pass per Pitfall 8).

Output:
- README.md at repo root (~120 lines)
- Wave 0 stubs in test_readme.py flip to GREEN (5/5 tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md
@.planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md
@.planning/phases/08-skill-quality-publish-bar/08-URL-LOCK.md
@SKILL.md
@assets/readme-template.md
@scripts/production_phase_writer.py
@scripts/tests/test_readme.py
@references/version-policy.md

<interfaces>
<!-- URL substitution from 08-URL-LOCK.md -->
- Read 08-URL-LOCK.md and extract the literal `RAW_INSTALL_URL:` value
- Read 08-URL-LOCK.md and extract `REPO_HTTPS_URL:` and `REPO_GIT_CLONE_URL:`
- If URL is `<TBD>` (option-defer chosen), README still ships with the literal `<TBD>` placeholder AND a callout banner pointing readers to 08-URL-LOCK.md (this case will fail QUAL-02 manual UAT but won't block plan completion — explicit, not a lie of omission)

NAMED_UPGRADES (verbatim from scripts/production_phase_writer.py:21-30):
- observability       (logs/metrics/traces via OpenTelemetry)
- migrations          (automated migrations via Drizzle Kit / Alembic)
- healthchecks        (/healthz endpoints)
- secret-manager      (secret manager integration)
- sentry              (Sentry error tracking)
- rate-limiting       (rate limiting middleware)
- backups             (backup strategy)

Dev-team role table (from SKILL.md lines 17-30 — copy verbatim, then expand each row with a one-line plain-English narration):

| Stage | Role | Delegates to | What you'll see in the terminal (NEW for README) |
|-------|------|--------------|--------------------------------------------------|
| 1. Intake | PM | /gsd:spec-phase | "PM is gathering requirements... ✓" |
| 2. Research | Architect | /brainiac, references/stack-menu.md | "Architect chose Next.js because..." |
| 3. Scaffold | DevOps | scripts/scaffold_dispatch.py | "DevOps is setting up Docker Compose..." |
| 4. Plan | Architect | /gsd:new-project --auto, /gsd:plan-phase | "Architect is planning phase 1..." |
| 5. Build | Frontend / Backend / DevOps | /gsd:execute-phase | "Frontend dev is building the homepage..." / "Backend dev is wiring the database..." |
| 6. Verify | QA + Reviewer | /code-tester, /gsd:verify-work, /predator, /gsd:code-review | "QA is running adversarial tests..." / "Reviewer is checking architecture..." |
| 7. Heal | Debug-cap | /gsd:debug, /problem-solver | (silent unless something breaks) |
| 8. Ship | DevOps | gh repo create --private | "DevOps is pushing to GitHub..." |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md (install + dev-team metaphor + demo + production-ready)</name>
  <files>README.md</files>
  <action>
    Read `.planning/phases/08-skill-quality-publish-bar/08-URL-LOCK.md` and extract the three URL values. Substitute them into the README content below. If a URL is `<TBD>`, leave the placeholder literal in the README AND add a banner near the install section pointing readers to 08-URL-LOCK.md.

    Create `README.md` at repo root with this structure (target ~120 lines):

    ```markdown
    # OSBuilder

    > Build end-to-end apps from a plain-English description. PM → Architect →
    > Frontend → Backend → DevOps → QA → Reviewer → Tech Writer, all running
    > sequentially over your existing Claude Code skill ecosystem (gsd, brainiac,
    > predator, code-tester, problem-solver). Ships to a private GitHub repo.

    OSBuilder is a Claude Code skill. You describe what you want; OSBuilder runs
    a virtual studio sequentially (never parallel — multi-agent is an explicit
    anti-feature) and delivers a working, version-controlled app.

    ## Quick Install (one-liner)

    ```bash
    curl -fsSL <RAW_INSTALL_URL> | sh
    ```

    Replaces nothing existing on your machine. Drops `~/.claude/skills/osbuilder/`
    in place and you're ready to run `/osbuilder` in any Claude Code session.

    ## Audited Install (read-the-script-first)

    For users whose security policy disallows `curl ... | sh`:

    ```bash
    git clone <REPO_GIT_CLONE_URL> ~/osbuilder-src
    cd ~/osbuilder-src && ./install.sh
    ```

    Same destination (`~/.claude/skills/osbuilder/`); same idempotent installer.

    ## How OSBuilder Works (the dev-team metaphor)

    OSBuilder narrates progress as a virtual studio. Each "role" is a sequential
    delegation to your existing skill ecosystem — never parallel, never multi-agent.

    | Stage | Role | What you'll see in the terminal | Delegates to |
    |-------|------|----------------------------------|--------------|
    | 1. Intake | **PM** | `PM is gathering requirements... ✓` | `/gsd:spec-phase` |
    | 2. Research | **Architect** | `Architect chose Next.js because…` | `/brainiac` + `references/stack-menu.md` |
    | 3. Scaffold | **DevOps** | `DevOps is setting up Docker Compose…` | `scripts/scaffold_dispatch.py` |
    | 4. Plan | **Architect** | `Architect is planning phase 1…` | `/gsd:new-project --auto` + `/gsd:plan-phase` |
    | 5. Build | **Frontend / Backend / DevOps** | `Frontend dev is building the homepage…` | `/gsd:execute-phase` |
    | 6. Verify | **QA + Reviewer** | `QA is running adversarial tests…` / `Reviewer is checking architecture…` | `/code-tester` + `/gsd:verify-work` + `/predator` + `/gsd:code-review` |
    | 7. Heal | **Debug-cap** (silent unless broken) | `Hit retry cap on validation; escalating to /problem-solver…` | `/gsd:debug` + `/problem-solver` |
    | 8. Ship | **DevOps** | `DevOps is pushing to GitHub…` | `gh repo create --private` |

    Every line you see in the terminal is plain English. Tutor mode (default)
    explains *what just happened* after each stage — disable with `--quiet`.

    ## 60-Second Demo

    ![OSBuilder demo — paragraph to private GitHub repo](assets/demo/osbuilder-demo.gif)

    Higher-quality version on asciinema:
    [`assets/demo/osbuilder-demo.cast`](assets/demo/osbuilder-demo.cast) — replay
    locally with `asciinema play assets/demo/osbuilder-demo.cast`.

    ## What you get

    - **Plain-English intake.** Describe an app in a paragraph; OSBuilder asks a
      few outcome-framed questions ("Should it work on phones too?") with an
      "I don't know, you decide" option that resolves to a sensible default.
    - **Auto-installed prereqs.** First run detects missing Node, Python, git,
      `gh`, and Docker; offers a single-confirmation auto-install.
    - **Deterministic scaffolders.** Always uses `create-next-app`, `cargo new`,
      etc. — never hand-writes `package.json` or `pyproject.toml` (avoids
      bolt.new's documented 10M-token spaghetti failure mode).
    - **Slopsquatting gate.** Every package install is verified against the
      public registry before any code runs (`--ignore-scripts` until verified).
    - **Private by default.** Repos created `gh repo create --private`; pass
      `--public` to override.
    - **Self-healing.** 4-class failure classifier with documented retry
      strategies and a hard 3-reflection cap before structured handoff to you.

    ## Modes

    | Flag | Effect |
    |------|--------|
    | (default) | Beginner mode — plain-English questioning, tutor narration on, sensible defaults |
    | `--advanced` | Exposes stack choice, deploy targets, scaffolder selection |
    | `--quiet` | Disables tutor mode (still narrates dev-team progress) |
    | `--no-docker` | SQLite-only single-user builds (for users without Docker Desktop) |
    | `--public` | Push to a public GitHub repo (default is private) |
    | `--production-ready` | Adds named ROADMAP phases for hardening (see below) |

    ## --production-ready

    Passes a flag that adds these as **named ROADMAP phases** (not default
    scaffold code). Phase 6 implements this; the flag is wired end-to-end
    through `intake_handler.py` → `state_writer.py` → `gsd_driver.py` step 3 →
    `production_phase_writer.py` (which emits the slash commands below).

    The 7 named upgrades:

    - `observability` — logs/metrics/traces via OpenTelemetry
    - `migrations` — automated migrations via Drizzle Kit / Alembic
    - `healthchecks` — `/healthz` endpoints
    - `secret-manager` — secret manager integration
    - `sentry` — Sentry error tracking
    - `rate-limiting` — rate limiting middleware
    - `backups` — backup strategy

    Default mode emits zero of these; the flag turns each into a
    `/gsd-add-phase <name>` command appended to your project's ROADMAP.

    ## Examples

    See [`examples/`](examples/) for 3-5 real apps OSBuilder built end-to-end —
    each with the original paragraph spec, screenshots, and a link to the
    resulting GitHub repo.

    ## Sub-skill version requirements

    OSBuilder declares minimum versions for its sub-skills in SKILL.md
    frontmatter (see `references/version-policy.md`). On first invocation each
    session, `scripts/check_skill_versions.py` validates that GSD, brainiac,
    predator, code-tester, and problem-solver meet those minimums. Drift =
    friendly upgrade command and refusal to proceed.

    To force a re-check after upgrading a sub-skill mid-session:

    ```bash
    rm -f ~/.osbuilder/last_check.txt
    ```

    ## Project status

    Built phase-by-phase via [GSD](https://github.com/<gsd-owner>/gsd) itself.
    See [`.planning/ROADMAP.md`](.planning/ROADMAP.md) for shipped phases and
    next milestones. Currently delivering Phase 8: Skill quality / publish-bar.

    ## License

    See LICENSE in the repo root.

    ## See also

    - [`SKILL.md`](SKILL.md) — entry point + routing (≤ 200 lines, enforced by CI)
    - [`references/`](references/) — playbooks, role briefs, refusal list, version policy
    - [`.planning/`](.planning/) — full GSD planning history
    - [`examples/`](examples/) — 3-5 reference builds

    ```

    **URL substitution rules (CRITICAL):**

    1. Read `.planning/phases/08-skill-quality-publish-bar/08-URL-LOCK.md`
    2. Extract the value after `RAW_INSTALL_URL:` and substitute for `<RAW_INSTALL_URL>` (one occurrence in the curl line)
    3. Extract the value after `REPO_GIT_CLONE_URL:` and substitute for `<REPO_GIT_CLONE_URL>` (one occurrence in the git clone line)
    4. If either value is the literal string `<TBD>`, leave the placeholder in place AND insert this banner directly above the `## Quick Install` heading:

    ```markdown
    > **NOTE:** The install one-liner below uses placeholder URLs. The publishing
    > URL has not been locked yet — see
    > `.planning/phases/08-skill-quality-publish-bar/08-URL-LOCK.md` for the
    > deferred-decision rationale. Until the URL is locked and the repo is
    > public, the audited-install path is the only working option.
    ```

    5. Replace `<gsd-owner>` (in the "Project status" GSD link) with `gsd-skill` placeholder if the user did not specify; this is GSD's own URL, not OSBuilder's, and is informational only.

    **Specificity sanity checks before saving:**
    - Confirm all 7 production-ready upgrades appear EXACTLY as `observability`, `migrations`, `healthchecks`, `secret-manager`, `sentry`, `rate-limiting`, `backups` (the test_readme.py:test_documents_production_ready greps for each verbatim).
    - Confirm all 8 roles appear (case-insensitive) — pm, architect, frontend, backend, devops, qa, reviewer, "tech writer".
    - Confirm `assets/demo/osbuilder-demo.gif` is referenced as a markdown image.
  </action>
  <read_first>
    - .planning/phases/08-skill-quality-publish-bar/08-URL-LOCK.md (URL values to substitute)
    - assets/readme-template.md (clone-and-run runbook structure analog)
    - SKILL.md (dev-team table at lines 17-30 — extend with terminal-narration column)
    - scripts/production_phase_writer.py (verify all 7 NAMED_UPGRADES — copy names verbatim)
    - scripts/tests/test_readme.py (test contract — what shapes the tests grep for)
    - .planning/phases/08-skill-quality-publish-bar/08-PATTERNS.md (README.md section, lines 571-636)
    - .planning/phases/08-skill-quality-publish-bar/08-RESEARCH.md (Pitfall 6 demo honesty, Pitfall 8 production-ready verification not re-implementation)
    - references/version-policy.md (cross-link target — confirm path)
  </read_first>
  <acceptance_criteria>
    - File `README.md` exists at repo root
    - File line count >= 80 (`wc -l README.md` reports >= 80)
    - File contains `# OSBuilder` H1 on line 1
    - File contains the literal regex `curl -fsSL.*install\.sh.*\| sh` (test_has_install_one_liner)
    - File contains all 8 dev-team role tokens (case-insensitive): `pm`, `architect`, `frontend`, `backend`, `devops`, `qa`, `reviewer`, `tech writer` (test_has_dev_team_section)
    - File references the demo asset: regex `assets/demo/osbuilder-demo\.(gif|cast)` matches (test_links_demo)
    - File contains `--production-ready` literal (test_documents_production_ready)
    - File contains all 7 NAMED_UPGRADES verbatim (case-insensitive): `observability`, `migrations`, `healthchecks`, `secret-manager`, `sentry`, `rate-limiting`, `backups`
    - All 5 Wave 0 tests pass: `uv run pytest scripts/tests/test_readme.py::test_has_install_one_liner scripts/tests/test_readme.py::test_has_dev_team_section scripts/tests/test_readme.py::test_links_demo scripts/tests/test_readme.py::test_documents_production_ready -x` reports 4 passed (test_demo_asset_present skips if 08-07 not yet done — that's expected and OK)
    - URL substitution: if 08-URL-LOCK.md `RAW_INSTALL_URL` is a real URL, README contains it verbatim; if `<TBD>`, README contains the deferred-decision banner
    - SKILL.md is NOT modified (this plan only edits README.md)
    - install.sh is NOT modified (existing 79-line installer is untouched)
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && wc -l README.md | awk '{exit ($1 >= 80) ? 0 : 1}' && head -1 README.md | grep -q "^# OSBuilder" && grep -qE "curl -fsSL.*install\.sh.*\| sh" README.md && for role in pm architect frontend backend devops qa reviewer "tech writer"; do grep -qi "$role" README.md || { echo "missing role: $role"; exit 1; }; done && grep -qE "assets/demo/osbuilder-demo\.(gif|cast)" README.md && grep -q -- "--production-ready" README.md && for u in observability migrations healthchecks secret-manager sentry rate-limiting backups; do grep -qi "$u" README.md || { echo "missing upgrade: $u"; exit 1; }; done && uv run pytest scripts/tests/test_readme.py::test_has_install_one_liner scripts/tests/test_readme.py::test_has_dev_team_section scripts/tests/test_readme.py::test_links_demo scripts/tests/test_readme.py::test_documents_production_ready -x</automated>
  </verify>
  <done>
    README.md exists at repo root with all 5 required sections; URL substituted from 08-URL-LOCK.md; all 5 grep-verifiable invariants hold; 4/5 Wave 0 tests pass (test_demo_asset_present may skip pending 08-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 08-URL-LOCK.md → README.md | URL value flows from a planning artifact into committed text |
| Public README → install.sh fetched via curl-pipe-shell | T-08-15 from 08-RESEARCH.md applies to the URL itself, not to README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Information Disclosure | README contains a real GitHub user/org name | accept | The URL is locked at 08-01 with explicit user consent; published README is by definition public |
| T-08-19 | Tampering | URL substitution accidentally produces an http:// (not https://) link | mitigate | Acceptance criteria: regex `curl -fsSL.*install\.sh` plus secondary check that the substituted URL begins with `https://` |
| T-08-20 | Repudiation | --production-ready section drifts from production_phase_writer.NAMED_UPGRADES | mitigate | Acceptance criteria greps for all 7 verbatim names; SC-6 verification (Pitfall 8) is the documentation pass that catches drift |
</threat_model>

<verification>
- README.md exists at repo root with line count >= 80
- All 5 sections present (install one-liner + audited install + dev-team metaphor + demo + --production-ready)
- All 8 roles + all 7 upgrades verbatim
- 4/5 Wave 0 tests in test_readme.py GREEN (test_demo_asset_present pending 08-07)
- URL substituted from 08-URL-LOCK.md (or deferred-decision banner inserted)
</verification>

<success_criteria>
- QUAL-02 success criterion 2: install one-liner documented in published README
- QUAL-03 success criterion 3: README has dev-team metaphor section + demo video link
- SC-6: README documents `--production-ready` flag and 7 named phases (verifies Phase 6 implementation per Pitfall 8)
</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-quality-publish-bar/08-06-SUMMARY.md` documenting:
- Final README line count
- URL substitution outcome (real URL or `<TBD>` deferred-decision banner)
- Pytest results (4/5 passing; test_demo_asset_present pending 08-07)
- All 7 production-ready upgrades verified verbatim
</output>
