---
phase: 07-additional-playbooks
plan: 06
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05]
files_modified:
  - scripts/tests/test_e2e_playbooks.py
  - scripts/tests/conftest.py
  - pyproject.toml
  - .planning/phases/07-additional-playbooks/07-HUMAN-UAT.md
autonomous: false
requirements: [SCAF-02, SCAF-03, SCAF-04, SCAF-05]
tags: [e2e, parametrized, stranger-clone, uat, phase-7]

must_haves:
  truths:
    - "A single parametrized test runs the 5-step contract (intake → scaffold → install → boot → stop) over [ai-service, cli, desktop, hub] (D-17)"
    - "Per-playbook timeout dict prevents flakes — desktop install gets 120s (Pitfall 8); ai-service gets 60s; cli gets 30s; hub gets 5s (D-18)"
    - "07-HUMAN-UAT.md mirrors 06-HUMAN-UAT.md format with 4 stranger-clone rows (one per playbook) + 1 Electron-refusal row + 1 /summarize Pydantic v2 smoke row (D-19)"
    - "@pytest.mark.skipif gates skip cleanly on machines without uv/pnpm/cargo (Pitfall 8 + RESEARCH.md A6 — Windows TimeoutExpired)"
  artifacts:
    - path: "scripts/tests/test_e2e_playbooks.py"
      provides: "parametrized 5-step E2E test across 4 playbooks; uses _real_run capture; cross-platform process group teardown"
      contains: "pytest.mark.parametrize"
      min_lines: 150
    - path: "scripts/tests/conftest.py"
      provides: "(extended) optional new fixtures for per-playbook setup; existing fixtures reused"
    - path: "pyproject.toml"
      provides: "(extended) [tool.pytest.ini_options].markers includes 'slow' for opt-out"
      contains: "slow"
    - path: ".planning/phases/07-additional-playbooks/07-HUMAN-UAT.md"
      provides: "6-test UAT file (4 stranger-clone + 1 Electron-refusal + 1 /summarize smoke)"
  key_links:
    - from: "scripts/tests/test_e2e_playbooks.py"
      to: "scripts/scaffold_dispatch.py + scripts/intake_handler.py"
      via: "real subprocess (NOT mocked); calls scripts directly with sys.executable"
      pattern: "subprocess\\.run.*INTAKE"
    - from: "scripts/tests/test_e2e_playbooks.py"
      to: "_real_run = subprocess.run captured before any monkeypatch"
      via: "Phase 4 STATE.md recursion-safe pattern"
      pattern: "_real_run = subprocess.run"
    - from: ".planning/phases/07-additional-playbooks/07-HUMAN-UAT.md"
      to: "Phase 6 06-HUMAN-UAT.md format"
      via: "expected/test/why_human/result 4-line per-test shape"
      pattern: "expected:|test:|why_human:|result:"
---

<objective>
Add the shared E2E harness for Phase 7: a single parametrized pytest file that runs the 5-step contract (intake → scaffold → install → boot → stop) across all 4 new playbooks (ai-service / cli / desktop / hub-platform) per D-17. Provide per-playbook timeout dict to handle Pitfall 8 (cold Cargo fetches on desktop). Ship `07-HUMAN-UAT.md` mirroring Phase 6's UAT format with 6 tests covering the human-only "stranger-clone in ≤5min" gate per playbook + Electron-refusal + /summarize Pydantic v2 smoke (D-19).

Purpose: SC-05 requires "every playbook in this phase passes the same end-to-end clone-and-run verification used in Phase 6 (a stranger clones, runs the documented command, reaches a working app in ≤ 5 minutes)." 07-02..07-05 each add one playbook with its own unit tests. This plan provides the SHARED contract verification — a single test file proves the contract is uniform across playbooks; a single UAT file documents the human-judged stranger-clone gates. Marked as `autonomous: false` because the test file's @pytest.mark.skipif gates and the HUMAN-UAT.md require user-machine verification (real `uv` / `pnpm` / `cargo` must be present, and the stranger-clone test is human-judged).

Output:
- `scripts/tests/test_e2e_playbooks.py` (NEW; ~250 lines per RESEARCH.md Code Example 4 verbatim contract)
- `scripts/tests/conftest.py` (EXTEND — only if needed for new fixtures; per RESEARCH.md prefer parametrize on the test itself)
- `pyproject.toml` (EXTEND — register `slow` marker so `pytest -m 'not slow'` skips this file)
- `.planning/phases/07-additional-playbooks/07-HUMAN-UAT.md` (NEW; mirrors 06-HUMAN-UAT.md format; 6 tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-additional-playbooks/07-CONTEXT.md
@.planning/phases/07-additional-playbooks/07-RESEARCH.md
@.planning/phases/07-additional-playbooks/07-PATTERNS.md
@.planning/phases/07-additional-playbooks/07-VALIDATION.md
@.planning/phases/06-ship-to-private-github-scalable-defaults/06-HUMAN-UAT.md
@scripts/tests/test_gh_handoff.py
@scripts/tests/conftest.py
@pyproject.toml

<interfaces>
<!-- VERBATIM contract from RESEARCH.md Code Example 4 (lines 836-953) -->

Per-playbook timeout dict (RESEARCH.md lines 856-861):
```python
TIMEOUTS = {
    "ai-service": {"install": 60, "boot": 30},
    "cli":         {"install": 30, "boot": 15},
    "desktop":     {"install": 120, "boot": 60},
    "hub":         {"install": 5,  "boot": 5},
}
```

Per-playbook boot command dict (RESEARCH.md lines 863-868):
```python
PLAYBOOK_BOOT_CMD = {
    "ai-service": ["uv", "run", "fastapi", "dev", "--port", "0"],
    "cli":         None,
    "desktop":     ["pnpm", "tauri", "dev"],
    "hub":         None,
}
```

Cross-platform process group teardown (RESEARCH.md lines 928-944):
```python
with subprocess.Popen(
    boot_cmd, cwd=str(project_dir),
    stdout=subprocess.PIPE, stderr=subprocess.PIPE,
    preexec_fn=os.setsid if os.name != "nt" else None,
) as proc:
    try:
        time.sleep(min(TIMEOUTS[playbook]["boot"], 8))
        if os.name != "nt":
            os.killpg(os.getpgid(proc.pid), signal.SIGTERM)
        else:
            proc.terminate()
        proc.wait(timeout=5)
    except subprocess.TimeoutExpired:
        proc.kill()
        pytest.fail(f"{playbook} boot did not stop cleanly")
```

_real_run capture pattern (RESEARCH.md line 27 of test_gh_handoff.py + STATE.md recursion-safe rule):
```python
import subprocess
_real_run = subprocess.run  # captured BEFORE any monkeypatch — recursion-safe
```

Parametrize signature (RESEARCH.md lines 876-882):
```python
@pytest.mark.parametrize("playbook,intake_text", [
    ("ai-service", "I want an HTTP API that summarizes documents with an LLM"),
    ("cli",         "I want a command-line tool to organize my photo library"),
    ("desktop",     "I want a desktop app that runs locally with a tray icon"),
    ("hub",         "build me a hub like Professor Hub for grading and rostering"),
])
def test_e2e_playbook(tmp_project_root, playbook, intake_text):
    ...
```

From 06-HUMAN-UAT.md (the Phase 6 UAT format mirrored here):
```markdown
--- # frontmatter open
status: complete
phase: 06-ship-to-private-github-scalable-defaults
source: [06-VERIFICATION.md]
started: 2026-05-01T23:00:00Z
updated: 2026-05-01T23:45:00Z
--- # frontmatter close

## Current Test
[testing complete]

## Tests

### 1. Stranger clone-and-run on fresh machine
expected: Working app reachable on localhost within 5 minutes; no edits to source files required.
test: On a second machine (or clean container), clone the repo OSBuilder just created and follow README Quick Start verbatim — `cd <dir> && cp .env.example .env && pnpm install && pnpm dev`.
why_human: Requires a fresh machine + a real GitHub repo created by a build run.
result: <pending>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/tests/test_e2e_playbooks.py + register `slow` marker in pyproject.toml</name>
  <files>scripts/tests/test_e2e_playbooks.py, pyproject.toml</files>
  <read_first>
    - .planning/phases/07-additional-playbooks/07-RESEARCH.md §"Code Example 4" (lines 836-953 — verbatim test body)
    - scripts/tests/test_gh_handoff.py (line 27 — `_real_run = subprocess.run` capture pattern)
    - pyproject.toml (existing `[tool.pytest.ini_options]` section — append `slow` to markers list)
    - scripts/tests/conftest.py (existing `tmp_project_root` fixture — reuse)
    - .planning/phases/07-additional-playbooks/07-RESEARCH.md §Pitfall 8 (per-playbook timeout dict); §"Assumptions Log A5" (Windows lacks os.killpg); §"Assumptions Log A6" (subprocess.TimeoutExpired Windows quirks)
    - .planning/phases/07-additional-playbooks/07-RESEARCH.md §"Per-Plan Implementation Notes — 07-06" (lines 1307-1325)
  </read_first>
  <action>
    **Step 1a — Create `scripts/tests/test_e2e_playbooks.py` using the VERBATIM body from RESEARCH.md Code Example 4 (lines 836-953):**

    Write the file with:
    1. Module docstring stating: "E2E parametrized test: intake → scaffold → install → boot → stop per playbook. Slow — gated by `@pytest.mark.slow` so quick `pytest -m 'not slow'` skips."
    2. Imports: `os`, `shutil`, `signal`, `subprocess`, `sys`, `time`, `from pathlib import Path`, `import pytest`
    3. `_real_run = subprocess.run` immediately after imports (recursion-safe per STATE.md)
    4. `REPO_ROOT = Path(__file__).resolve().parents[2]; SCAFFOLD = REPO_ROOT / "scripts" / "scaffold_dispatch.py"; INTAKE = REPO_ROOT / "scripts" / "intake_handler.py"`
    5. `TIMEOUTS` and `PLAYBOOK_BOOT_CMD` dicts verbatim from `<interfaces>`
    6. `_has(tool)` helper using `shutil.which`
    7. `pytestmark = pytest.mark.slow` at module level (so the whole file inherits the slow marker — `pytest -m 'not slow'` skips entirely)
    8. The single parametrized `test_e2e_playbook` per the verbatim contract

    Critical implementation detail — for the cli case, the "boot" step is `<name> --help`:
    ```python
    elif playbook == "cli":
        result = _real_run(
            ["uv", "run", project_name, "--help"],
            cwd=str(project_dir), capture_output=True, text=True,
            timeout=TIMEOUTS["cli"]["boot"], check=True,
        )
        assert "Usage:" in result.stdout or "Commands" in result.stdout
    ```

    For hub, install/boot are no-ops; the assertion is just "scaffold succeeded + structural diff against snapshot":
    ```python
    if playbook == "hub":
        # No install/boot. Verify structural diff against snapshot (reuses 07-05's _structural_signature).
        snapshot = REPO_ROOT / "assets" / "hub-template" / "professor-snapshot"
        if snapshot.exists():
            snapshot_dirs = {p.name for p in snapshot.iterdir() if p.is_dir() and not p.name.startswith(".")}
            built_dirs = {p.name for p in project_dir.iterdir() if p.is_dir() and not p.name.startswith(".")}
            assert snapshot_dirs.issubset(built_dirs) or built_dirs, \
                f"Hub structural mismatch: snapshot={snapshot_dirs}, built={built_dirs}"
        return
    ```

    For the intake step (step 1), invoke `intake_handler` via subprocess with `--input`:
    ```python
    proc = _real_run(
        [sys.executable, str(INTAKE), "parse", "--input", intake_text,
         "--project-root", str(tmp_project_root)],
        check=True, capture_output=True, text=True, timeout=10,
    )
    ```
    **Implementation note:** Read `scripts/intake_handler.py` argparse handling. If `parse` subcommand or `--input` flag don't yet exist (the existing intake handler may use different CLI shape — check at execution time), adapt the invocation accordingly. The test must exercise the actual production intake CLI surface, NOT a test-only pathway. If the existing CLI shape differs, the test SHOULD use that shape (e.g., piping text via stdin, or using the existing `parse_paragraph` script entry).

    **Step 1b — Register `slow` marker in `pyproject.toml`:**

    Read existing `[tool.pytest.ini_options]` block (`pyproject.toml`). If `markers` key exists, append `"slow: marks tests as slow (skipped by default; opt-in with -m slow)"`. If `markers` key doesn't exist, add it:
    ```toml
    [tool.pytest.ini_options]
    # ... existing keys ...
    markers = [
        "slow: marks tests as slow (skipped by default; opt-in with -m slow)",
    ]
    ```
    If a `markers` list already exists, append the entry (do NOT delete existing entries).

    **Verification:** The test file should COLLECT 4 parametrized cases without errors. Running `uv run pytest scripts/tests/test_e2e_playbooks.py --collect-only -q` should show 4 IDs (`test_e2e_playbook[ai-service-...]`, `[cli-...]`, `[desktop-...]`, `[hub-...]`). Running `uv run pytest -m 'not slow' --collect-only -q` should NOT include these.

    The file must NOT actually execute the real subprocess steps in `--collect-only` mode; collection should be fast (<1s).

    **Cross-platform note:** Add a comment block at the top of the test function explaining:
    ```python
    # Cross-platform notes (per RESEARCH.md A5/A6):
    # - Windows lacks os.killpg → fall back to proc.terminate() (graceful) then proc.kill() if needed.
    # - subprocess.TimeoutExpired on Windows + shell=False has known quirks; we use shell=False
    #   throughout (Phase 6 evidence at test_gh_handoff.py:27 — same pattern works there).
    ```
  </action>
  <verify>
    <automated>uv run pytest scripts/tests/test_e2e_playbooks.py --collect-only -q 2>&amp;1 | grep -E "test_e2e_playbook\\[" | wc -l | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/tests/test_e2e_playbooks.py` exists; `wc -l scripts/tests/test_e2e_playbooks.py` ≥ 150
    - File contains `_real_run = subprocess.run` (line near top, before any monkeypatch)
    - File contains `pytestmark = pytest.mark.slow` (or per-test `@pytest.mark.slow` on test_e2e_playbook)
    - File contains `TIMEOUTS = {` and the dict has all 4 keys (ai-service, cli, desktop, hub)
    - File contains `"desktop": {"install": 120` (Pitfall 8 — desktop is 2× others)
    - File contains `PLAYBOOK_BOOT_CMD = {`
    - File contains `@pytest.mark.parametrize("playbook,intake_text"`
    - File contains all 4 parametrize tuples (ai-service, cli, desktop, hub)
    - File contains `os.killpg` AND `proc.terminate()` (cross-platform teardown)
    - File contains `shutil.which` (skip-if-tool-missing gates)
    - `uv run pytest scripts/tests/test_e2e_playbooks.py --collect-only -q` shows 4 parametrized IDs
    - `uv run pytest -m 'not slow' --collect-only -q` does NOT include the 4 e2e IDs
    - `grep -c "slow:" pyproject.toml` returns ≥ 1 (marker registered)
    - `uv run pytest scripts/tests/ -x -q` exits 0 (full suite passes; e2e file skipped by default)
  </acceptance_criteria>
  <done>test_e2e_playbooks.py created with verbatim 4-case parametrize, per-playbook TIMEOUTS dict, cross-platform teardown, _real_run capture pattern, and slow marker; pyproject.toml registers the marker; full suite still GREEN with E2E gated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create 07-HUMAN-UAT.md mirroring 06-HUMAN-UAT.md format with 6 tests</name>
  <files>.planning/phases/07-additional-playbooks/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-ship-to-private-github-scalable-defaults/06-HUMAN-UAT.md (full file — format mirror)
    - .planning/phases/07-additional-playbooks/07-RESEARCH.md §"Manual-Only Verifications" (lines 1175-1182 — per-playbook stranger-clone rationale)
    - .planning/phases/07-additional-playbooks/07-CONTEXT.md §"Verification (SC-05)" (D-17..D-19)
    - ROADMAP.md Phase 7 §"Success Criteria" (SC-01..SC-05)
  </read_first>
  <what-built>
    The automated test_e2e_playbooks.py covers the contract (5-step intake→scaffold→install→boot→stop). 07-HUMAN-UAT.md is the human-judged complement — same UAT format Phase 6 used, listing 6 tests:
    1. Stranger clone-and-run for ai-service in ≤5min (SC-05 + SC-01 verification)
    2. Stranger clone-and-run for cli in ≤5min (SC-05 + SC-02)
    3. Stranger clone-and-run for desktop in ≤5min (SC-05 + SC-03; rust install excluded from 5min budget)
    4. Stranger clone-and-run for hub-platform — UX-judged "does the routing CLAUDE.md make sense to a fresh human" (SC-05 + SC-04)
    5. Refusal of "build me an Electron desktop app" produces friendly explanation pointing to Tauri rationale (SC-03 + D-22)
    6. /summarize POST endpoint stub responds correctly with valid Pydantic v2 request (SCAF-02 D-12 verification)
  </what-built>
  <how-to-verify>
    File creation and content review:

    1. Open `.planning/phases/07-additional-playbooks/07-HUMAN-UAT.md`
    2. Verify YAML frontmatter has: `status: pending`, `phase: 07-additional-playbooks`, `source: [07-VERIFICATION.md]`, `started: <blank>`, `updated: <blank>`
    3. Verify the file contains `## Current Test` and `## Tests` sections
    4. Verify exactly 6 tests are listed (numbered 1-6) — one per the items above
    5. Each test follows the verbatim 4-line shape:
       ```
       expected: <single sentence>
       test: <step-by-step instructions>
       why_human: <why this can't be automated>
       result: <pending>
       ```
    6. Confirm test #5 mentions "Electron" and "Tauri 2 alternative"
    7. Confirm test #6 includes the verbatim curl command: `curl -X POST http://127.0.0.1:8000/summarize -H 'Content-Type: application/json' -d '{"text":"hello world"}'`
    8. Confirm desktop test (#3) explicitly notes "Rust toolchain install is not counted in 5-min budget"

    User: type "approved" if the file matches the format and contains all 6 tests OR describe issues.
  </how-to-verify>
  <action>
    **Create `.planning/phases/07-additional-playbooks/07-HUMAN-UAT.md` mirroring 06-HUMAN-UAT.md format.**

    Use this verbatim structure:
    ```markdown
    ---
    status: pending
    phase: 07-additional-playbooks
    source: [07-VERIFICATION.md]
    started:
    updated:
    ---

    ## Current Test
    [pending — runner has not started]

    ## Tests

    ### 1. Stranger clone-and-run on fresh machine — ai-service
    expected: Working FastAPI app reachable on `http://127.0.0.1:8000/docs` within 5 minutes of `git clone`; no edits to source files required.
    test: On a second machine (or clean container) with `uv` already installed (preflight): clone the repo OSBuilder just created → `cd <dir> && uv sync && uv run fastapi dev` → open `http://127.0.0.1:8000/docs` → confirm `/`, `/health`, `/summarize` endpoints visible. Time the run from clone to /docs page.
    why_human: Requires a fresh machine + a real GitHub repo created by a build run; subjective UX timing. Automated test covers the 5-step contract; human gate covers the actual stranger-clone experience.
    result: <pending>

    ### 2. Stranger clone-and-run on fresh machine — cli
    expected: `uv run <app-name> --help` prints Rich-formatted help screen within 5 minutes of clone; `<app-name> ping` writes a row to SQLite and reads it back.
    test: On second machine with `uv` installed: clone → `cd <dir> && uv sync && uv run <app-name> --help` → confirm Rich-styled output → `uv run <app-name> ping` → confirm "ping #1" message + DB path → run again → confirm "ping #2" (state persisted across runs per SC-02).
    why_human: Subjective Rich-formatting verification + cross-run persistence behavior; can't be deterministically automated without a real shell.
    result: <pending>

    ### 3. Stranger clone-and-run on fresh machine — desktop
    expected: `pnpm tauri dev` opens a native window with hot-reload within 5 minutes of clone (Rust toolchain install not counted in the 5-min budget).
    test: On second machine with `pnpm` AND Rust (cargo+rustc) already installed via preflight: clone → `cd <dir> && pnpm install && pnpm tauri dev` → confirm a native window opens with the default Tauri+React content. Note: cold Cargo fetch can take 60-120s on first run (Pitfall 8); count this within budget on warm cache.
    why_human: OS-level GUI assertion not feasible in CI; window-opens-and-renders requires a graphical session and OS permissions (macOS Gatekeeper, Windows SmartScreen).
    result: <pending>

    ### 4. Stranger clone-and-run on fresh machine — hub-platform
    expected: A fresh human reading the top-level `CLAUDE.md` understands the hub's purpose and the role of each subfolder; routing-table format matches Professor Hub's structure.
    test: On second machine: clone → `cd <dir>` → open `CLAUDE.md` → confirm it lists each sub-tool subfolder with a "TODO — describe X" purpose row → open one sub-tool's `CLAUDE.md` → confirm it's a placeholder with a clear "re-run /osbuilder" next-step instruction.
    why_human: Hub-platform's success metric is comprehension by a fresh reader, not runtime behavior. UX measured by "does the routing make sense without prior context."
    result: <pending>

    ### 5. Electron refusal produces friendly Tauri-2 rationale
    expected: Submitting a spec containing "build me an Electron desktop app for X" produces a refusal pointing the user at Tauri 2 with the documented rationale (smaller binaries, less RAM); refusal copy is human-friendly (no jargon).
    test: Run OSBuilder with intake "I want an Electron desktop app for note-taking" → confirm OSBuilder refuses → confirm the refusal text mentions: (a) "Electron" by name, (b) "Tauri 2" as the alternative, (c) the rationale (~10MB vs 150MB binaries, ~50% less RAM, system WebView). Verify state.md `last_failure` matches the refusal pattern.
    why_human: Default-mode refusal copy is human-judged for friendliness; automated test only verifies the refuse-list.md text contains "Electron" and "Tauri" — actual UX evaluation is human.
    result: <pending>

    ### 6. /summarize endpoint smoke (Pydantic v2 verification)
    expected: After ai-service boot, POST to /summarize with valid JSON returns the summary stub (text[:200]); request validation fires on invalid input (e.g., empty text); response uses Pydantic v2 patterns (no v1 silent no-op per Pitfall 4).
    test: After ai-service boot: `curl -X POST http://127.0.0.1:8000/summarize -H 'Content-Type: application/json' -d '{"text":"hello world"}'` → confirm response is `{"summary":"hello world"}`. Then: `curl -X POST http://127.0.0.1:8000/summarize -H 'Content-Type: application/json' -d '{"text":""}'` → confirm 422 error (Pydantic v2 Field min_length validation fires; v1 syntax would have silently accepted).
    why_human: Real HTTP smoke against a booted server requires the server to be running; automated test covers Pydantic v2 syntax via AST grep but doesn't exercise live request validation.
    result: <pending>

    ## Notes for the human runner

    - Run tests in order. Tests 1-4 are independent; test 5 doesn't require a built repo (just an intake run); test 6 requires test 1's app to be booted.
    - The 5-minute budget excludes one-time toolchain install (`uv`, `pnpm`, `cargo`) — preflight handles that as a separate confirmation. The budget covers `clone → install → run → reach the documented endpoint`.
    - For test 3, if the Tauri window does NOT open, capture stderr and check for: missing webkit2gtk-4.1-dev (Linux), missing MSVC (Windows — Pitfall 3), missing Xcode CLT (macOS).
    - For test 5, the refusal text comes from `references/refuse-list.md`; if the rationale is missing/incomplete, file an issue against 07-01 (the migration plan).
    - Mark `result: passed` / `result: failed: <description>` per row. Update `status: in-progress` while running; `status: complete` when all 6 done.
    ```

    Reformat as needed to match the EXACT line shape of `06-HUMAN-UAT.md` (re-read at execution time). The 4-line per-test shape (`expected:` / `test:` / `why_human:` / `result:`) is mandatory for tooling that parses these files.
  </action>
  <verify>
    Manual review of the file by the user. The user types "approved" or describes issues.
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-additional-playbooks/07-HUMAN-UAT.md` exists
    - File has YAML frontmatter with `status: pending`, `phase: 07-additional-playbooks`
    - File contains exactly 6 numbered test sections (`### 1.` through `### 6.`)
    - Each test section contains all 4 line types: `expected:`, `test:`, `why_human:`, `result:`
    - File contains the literal string "Electron" (test 5)
    - File contains the literal string "Tauri 2" (test 5)
    - File contains the literal string `curl -X POST http://127.0.0.1:8000/summarize` (test 6)
    - File contains the literal string "Rust toolchain install is not counted" (test 3)
    - User has reviewed and typed "approved" OR provided correction guidance
  </acceptance_criteria>
  <resume-signal>Type "approved" if the file matches format and contains 6 tests, OR describe specific issues to fix.</resume-signal>
  <done>07-HUMAN-UAT.md created mirroring 06-HUMAN-UAT.md format; 6 tests covering the 4 stranger-clone gates + Electron-refusal + /summarize smoke; user approved or corrections applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real subprocess via `_real_run` → user machine | E2E test runs REAL `uv` / `pnpm` / `cargo` subprocesses on the test machine; gated by `@pytest.mark.skipif(not _has(...))` |
| Real subprocess output → test process stdout/stderr | No untrusted source — output is from trusted toolchain binaries |
| Cross-platform process group teardown | POSIX `os.killpg` vs Windows `proc.terminate()` divergence documented inline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Tampering | Real `pnpm/uv/cargo` subprocess execution in test environment | medium | mitigate | Tests use `shell=False` argv lists throughout; `_real_run` is a captured reference (recursion-safe); skip-if-tool-missing gates prevent accidental shell-out without expected toolchain. |
| T-07-06-02 | Denial of Service | Long-running install (cold Cargo fetch) blocks CI | medium | mitigate | Per-playbook `TIMEOUTS` dict (Pitfall 8): desktop install gets 120s; ai-service 60s; cli 30s; hub 5s. `subprocess.TimeoutExpired` raises `pytest.fail` with diagnostic message. |
| T-07-06-03 | Tampering | Boot subprocess (fastapi dev / pnpm tauri dev) keeps running after test | medium | mitigate | Cross-platform teardown: POSIX `os.killpg(os.getpgid(proc.pid), SIGTERM)` kills the whole process group; Windows `proc.terminate()` + `proc.wait(timeout=5)`; `proc.kill()` fallback on TimeoutExpired. Documented inline per RESEARCH.md A5. |
| T-07-06-04 | Information Disclosure | Test output captures real toolchain stdout/stderr; could leak local paths | low | accept | `tmp_project_root` fixture uses `pytest`'s tmp_path, which is isolated and per-test. Output is captured but not persisted. CI logs are private to the contributor. |
| T-07-06-05 | Spoofing | Wrong `_real_run` reference (e.g., post-monkeypatch) → recursion / wrong subprocess | low | mitigate | Capture `_real_run = subprocess.run` at MODULE TOP (before any test-level monkeypatch); STATE.md "Recursion-safe monkeypatch" rule + Phase 4 evidence at `test_gh_handoff.py:27`. |
| T-07-06-06 | Tampering | UAT file `result:` field manipulation | low | accept | UAT is a human-judged document; `result:` is filled in by the runner. Git history records changes; tampering visible in PR review. |

**Block on high:** none. Three medium threats — all mitigated by per-playbook timeouts (T-02), cross-platform teardown (T-03), and module-top capture (T-05).
</threat_model>

<verification>
- `uv run pytest scripts/tests/test_e2e_playbooks.py --collect-only -q` shows 4 parametrized IDs
- `uv run pytest -m 'not slow' --collect-only -q` does NOT include the 4 IDs (slow marker working)
- `uv run pytest scripts/tests/ -x -q` exits 0 (full suite passes with E2E skipped by default)
- `wc -l scripts/tests/test_e2e_playbooks.py` ≥ 150
- 07-HUMAN-UAT.md has 6 tests in the 4-line shape
- User has reviewed the UAT file and approved
- (Optional, machine-dependent) `uv run pytest -m slow scripts/tests/test_e2e_playbooks.py -x -v` runs all 4 cases when toolchain is installed (this is the SC-05 contract verification — but only on machines with full preflight)
</verification>

<success_criteria>
- D-17 implemented: single parametrized E2E file covers ai-service / cli / desktop / hub-platform
- D-18 implemented: per-playbook TIMEOUTS dict (Pitfall 8 — desktop=120s)
- D-19 implemented: 07-HUMAN-UAT.md mirrors 06-HUMAN-UAT.md with 6 tests
- _real_run capture pattern + cross-platform teardown documented inline
- `slow` marker registered in pyproject.toml; default `pytest` run skips this file
- All 4 SCAF-* requirements (SCAF-02..SCAF-05) covered by E2E parametrize
</success_criteria>

<output>
After completion, create `.planning/phases/07-additional-playbooks/07-06-SUMMARY.md` with:
- Files created/modified (with line counts)
- D-17, D-18, D-19 implementation status
- Test count delta (+4 parametrized IDs in test_e2e_playbooks.py; +6 manual UAT tests)
- Pitfalls confirmed mitigated (8 — per-playbook timeout dict; A5/A6 — cross-platform teardown)
- Note on E2E execution: full SC-05 verification requires a machine with `uv`, `pnpm`, `cargo` installed (or a CI matrix with preflight); the file is skipped by default to keep the standard `pytest` run fast (~25s per Phase 6 baseline).
</output>
