---
phase: 03-intake-stack-research-web-playbook-one-playbook-e2e
plan: 05
type: execute
wave: 2
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - references/playbooks/web.md
  - references/stack-menu.md
  - references/question-bank.md
autonomous: true
requirements: [SCAF-01, RES-03, IN-03, IN-04]

must_haves:
  truths:
    - "references/playbooks/web.md exists and documents the verified create-next-app command with all 7 flags"
    - "references/playbooks/web.md lists the 4 post-scaffold files OSBuilder writes and the files it must NOT write"
    - "references/stack-menu.md exists with a structured table of web defaults with verified versions"
    - "references/question-bank.md exists with ≥ 5 plain-English questions — no jargon words"
    - "Every question in question-bank.md has an 'I don't know, you decide' option"
    - "test_questions_have_no_jargon flips from SKIPPED to GREEN after this plan ships"
    - "test_web_playbook_exists flips from SKIPPED to GREEN after this plan ships"
    - "test_questions_have_you_decide_option flips from SKIPPED to GREEN after this plan ships"
  artifacts:
    - path: "references/playbooks/web.md"
      provides: "Web playbook spec: scaffold command, Drizzle wiring, refusals, stack versions"
      contains: "pnpm create next-app"
      min_lines: 40
    - path: "references/stack-menu.md"
      provides: "Fallback stack defaults table with verified versions for stack_researcher.py"
      contains: "next.js"
      min_lines: 30
    - path: "references/question-bank.md"
      provides: "≥ 5 plain-English outcome-framed questions with 'I don't know, you decide' defaults"
      contains: "I don't know, you decide"
      min_lines: 40
  key_links:
    - from: "references/stack-menu.md"
      to: "scripts/stack_researcher.py _read_stack_menu()"
      via: "parsed by _read_stack_menu() when brainiac times out (RES-03 fallback path)"
      pattern: "## Web playbook defaults"
    - from: "references/question-bank.md"
      to: "SKILL.md PM role (runtime)"
      via: "loaded on-demand during intake; question text presented to user"
      pattern: "## Q:"
    - from: "references/playbooks/web.md"
      to: "scripts/scaffold_dispatch.py (implementation reference)"
      via: "documents what scaffold_dispatch.py must produce; script is the consumer"
      pattern: "create next-app"
---

<objective>
Write the three reference files that complete Phase 3: `references/playbooks/web.md`, `references/stack-menu.md`, and `references/question-bank.md`.

Purpose: These files are the human-readable specification layer and runtime references for the intake → research → scaffold pipeline. Without them, the wave 1 scripts are missing their configuration: `stack_researcher.py` has no fallback file, `test_questions_have_no_jargon` and `test_questions_have_you_decide_option` remain SKIPPED, and `test_web_playbook_exists` remains SKIPPED.

Output:
- `references/playbooks/web.md`: playbook spec — scaffold command, post-scaffold files, refuse list, stack versions.
- `references/stack-menu.md`: fallback table — verified versions for all 5 stack components.
- `references/question-bank.md`: question bank — ≥ 5 jargon-free questions with 3-option format.

These files flip 3 remaining SKIPPED test stubs to GREEN: `test_web_playbook_exists`, `test_questions_have_no_jargon`, `test_questions_have_you_decide_option`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-RESEARCH.md
@.planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-PATTERNS.md
@scripts/tests/test_intake.py
@scripts/tests/test_scaffold_dispatch.py

<interfaces>
<!-- Key assertions the reference files MUST satisfy. Extracted from test bodies. -->

From test_scaffold_dispatch.py::test_web_playbook_exists:
```python
content = playbook_path.read_text(encoding="utf-8")
assert "pnpm create next-app" in content          # command must be documented
assert "--typescript" in content                   # flags must be listed
assert "--disable-git" in content                  # pitfall 2 mitigation documented
assert "drizzle" in content.lower()               # Drizzle wiring steps documented
assert "compose.yaml" in content                  # Compose v2 filename documented
```

From test_intake.py::test_questions_have_no_jargon:
```python
JARGON = {
    "responsive", "ORM", "framework", "endpoint", "middleware",
    "hydration", "SSR", "SSG", "CDN", "schema", "migration",
}
# None of these words (case-insensitive, word-boundary matched) may appear
```

From test_intake.py::test_questions_have_you_decide_option:
```python
question_sections = re.split(r"^## Q:", content, flags=re.MULTILINE)
# Every ## Q: section must match:
re.search(r"I don.t know,? you decide", section, re.IGNORECASE)
```

From scripts/stack_researcher.py _read_stack_menu():
```python
# Parses rows in "## Web playbook defaults" section:
rows = re.findall(
    r"^\|\s*(\w[\w\s]*?)\s*\|\s*([\w@./\-]+)\s*\|\s*([\w.\-]+)\s*\|",
    content,
    re.MULTILINE,
)
# component_map = {"framework": "framework", "orm": "orm", "database": "database",
#                  "css": "css", "package manager": "package_manager"}
```
The stack-menu.md table MUST have a section header "## Web playbook defaults" and table rows where column 1 matches the component_map keys exactly.

Verified stack versions (from RESEARCH.md Standard Stack — npm registry verified 2026-04-30):
- create-next-app / next.js: 16.2.4
- drizzle-orm: 0.45.2
- drizzle-kit: 0.31.10
- postgres (postgres.js): 3.4.9
- pnpm: 10.33.2
- tailwindcss: 4.2.4
- Postgres Docker image: postgres:18-alpine

Reference doc structure pattern (from PATTERNS.md — references/preflight/README.md analog):
- Lead with blockquote: "Loaded on-demand. NOT pulled into SKILL.md (≤ 200 lines)."
- Use ## sections with imperative-noun headers
- Tables for structured data (versions, matrices)
- End with "See also" cross-references
- Keep each reference doc under ~80 lines
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/playbooks/web.md — web playbook specification</name>
  <files>references/playbooks/web.md</files>
  <read_first>
    - .planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-RESEARCH.md (Pattern 1 — verified create-next-app command; Pattern 2 — post-scaffold files list and contents; Pitfall 3 — files OSBuilder must NOT write; Standard Stack table with verified versions; Security Domain — path traversal/shell injection refusals)
    - .planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-PATTERNS.md (section "references/playbooks/web.md" — document structure pattern and section headers)
    - scripts/tests/test_scaffold_dispatch.py (test_web_playbook_exists assertions — these are what the file must satisfy to flip GREEN)
    - references/preflight/README.md (structural analog — blockquote lead, ## sections, tables, See also footer)
  </read_first>
  <action>
    Create `references/playbooks/web.md` by running `mkdir -p references/playbooks` first if the directory does not exist.

    The file MUST satisfy all assertions in `test_web_playbook_exists`:
    - "pnpm create next-app" appears in the file
    - "--typescript" appears in the file
    - "--disable-git" appears in the file
    - "drizzle" appears (case-insensitive)
    - "compose.yaml" appears

    Write the file with this content (adapt section contents from RESEARCH.md, keep structure from PATTERNS.md):

    ```markdown
    # OSBuilder Web Playbook

    > Specification for the web scaffold path (`app_type: web`).
    > Loaded on-demand by the Architect role. NOT pulled into SKILL.md
    > (line limit ≤ 200; see Phase 1 Plan 01-02).

    ## What the web playbook produces

    A runnable Next.js 16 + Drizzle + Postgres + Tailwind 4 project on disk.
    The user can `pnpm install && pnpm dev` and see the scaffolded homepage
    on `localhost:3000` — with zero hand-written `package.json` or `tsconfig.json`.

    ## Scaffold command (non-interactive)

    ```bash
    pnpm create next-app@latest <project-name> \
      --typescript \
      --tailwind \
      --app \
      --src-dir \
      --eslint \
      --use-pnpm \
      --disable-git \
      --import-alias "@/*"
    ```

    Source: nextjs.org/docs/app/api-reference/cli/create-next-app (verified 2026-04-30).
    Note: `--disable-git` prevents a nested git init inside the scaffold directory (Pitfall 2).
    Note: `--yes` is NEVER used — it reads saved machine preferences and may pick wrong defaults.

    ## Post-scaffold files written by OSBuilder

    After `create-next-app` runs, OSBuilder writes exactly these 4 files:

    | File | Purpose |
    |------|---------|
    | `src/lib/db.ts` | Drizzle + postgres.js connection (3 lines) |
    | `drizzle.config.ts` | Drizzle Kit config with postgresql dialect |
    | `.env.example` | `DATABASE_URL=` placeholder (committed; not secret) |
    | `compose.yaml` | Docker Compose v2: postgres:18-alpine service |

    ## Files OSBuilder must NOT write

    create-next-app already produces these — OSBuilder touching them causes conflicts:

    `package.json`, `tsconfig.json`, `.eslintrc.json` (or `eslint.config.mjs`),
    `next.config.ts`, `postcss.config.mjs`, `tailwind.config.ts`, `README.md`,
    `src/app/` directory.

    ## Refuse list

    In v1 default builds, OSBuilder refuses:

    - Kubernetes / Helm / service mesh
    - Microservices architecture
    - Electron (use Tauri 2 via desktop playbook)
    - Auto-deploy without explicit consent
    - Public GitHub repos by default (use `--public` to override)

    ## Stack (pinned versions — verified 2026-04-30)

    | Component | Package | Version | Notes |
    |-----------|---------|---------|-------|
    | Scaffold | create-next-app | 16.2.4 | includes Next.js 16 + React 19.2 + TS 5.x |
    | CSS | tailwindcss | 4.2.4 | CSS-first config via `@theme`; no tailwind.config.js |
    | ORM | drizzle-orm | 0.45.2 | lighter than Prisma; type-safe; locked decision |
    | Migrations | drizzle-kit | 0.31.10 | companion to drizzle-orm |
    | DB driver | postgres (postgres.js) | 3.4.9 | recommended for serverless/edge |
    | Package mgr | pnpm | 10.33.2 | `--use-pnpm` flag in create-next-app |
    | Database | postgres Docker | 18-alpine | pinned; `compose.yaml` service name: `postgres` |

    ## See also

    - `references/stack-menu.md` — fallback defaults for stack_researcher.py
    - `references/question-bank.md` — plain-English intake questions
    - `scripts/scaffold_dispatch.py` — implementation that reads this spec
    - `RESEARCH.md` Phase 3 — verification sources and pitfall catalog
    ```

    Note on triple-backtick fences in the markdown: the bash code block inside the file uses triple backticks. Write the file with literal triple-backtick code fences. The Write tool accepts literal markdown content.
  </action>
  <verify>
    <automated>cd "/Users/charlie/Documents/Work & Projects/VSCode Projects/OSBuilder" && python3 -m pytest scripts/tests/test_scaffold_dispatch.py::test_web_playbook_exists -x --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f references/playbooks/web.md` exits 0
    - `grep -q "pnpm create next-app" references/playbooks/web.md` exits 0
    - `grep -q "\-\-typescript" references/playbooks/web.md` exits 0
    - `grep -q "\-\-disable-git" references/playbooks/web.md` exits 0
    - `grep -qi "drizzle" references/playbooks/web.md` exits 0
    - `grep -q "compose.yaml" references/playbooks/web.md` exits 0
    - `grep -q "docker-compose.yml" references/playbooks/web.md` exits NON-zero (deprecated name must not be recommended)
    - `grep -q "\-\-yes" references/playbooks/web.md` exits NON-zero (--yes must not be in the recommended command)
    - `grep -q "Loaded on-demand" references/playbooks/web.md` exits 0 (SKILL.md integration note)
    - `grep -q "See also" references/playbooks/web.md` exits 0 (cross-reference section)
    - `python3 -m pytest scripts/tests/test_scaffold_dispatch.py::test_web_playbook_exists -x --tb=short` exits 0 (GREEN — stub flips from SKIPPED to GREEN)
    - `wc -l < references/playbooks/web.md` reports ≤ 80 (reference doc size limit)
  </acceptance_criteria>
  <done>
    references/playbooks/web.md exists with scaffold command, post-scaffold files list, forbidden files list, refuse list, pinned stack versions, See also footer. test_web_playbook_exists is GREEN (no longer SKIPPED).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create references/stack-menu.md and references/question-bank.md</name>
  <files>references/stack-menu.md, references/question-bank.md</files>
  <read_first>
    - scripts/tests/test_intake.py (test_questions_have_no_jargon — full JARGON set; test_questions_have_you_decide_option — regex pattern `r"I don.t know,? you decide"` and section split by `## Q:`)
    - scripts/stack_researcher.py (_read_stack_menu — the parsing regex and component_map; the file must parse correctly)
    - .planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-PATTERNS.md (section "references/stack-menu.md" — table format; section "references/question-bank.md" — question format with 3-option bullets)
    - .planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-RESEARCH.md (Pattern 4 — question format; Standard Stack versions; "Never appears in questions" jargon ban list)
  </read_first>
  <action>
    Create two files.

    ===== FILE A: references/stack-menu.md =====

    CRITICAL: The table in "## Web playbook defaults" section MUST parse correctly via stack_researcher.py's _read_stack_menu() regex:
    ```python
    re.findall(r"^\|\s*(\w[\w\s]*?)\s*\|\s*([\w@./\-]+)\s*\|\s*([\w.\-]+)\s*\|", content, re.MULTILINE)
    ```
    And component_map keys: "framework", "orm", "database", "css", "package manager" (lowercase).

    Column 1 of each data row MUST be one of: Framework, ORM, Database, CSS, Package Manager — the regex lowercases them via component_map lookup.

    ```markdown
    # OSBuilder Stack Menu — Web Playbook Defaults

    > Fallback defaults loaded by `stack_researcher.py` when `/brainiac` times out
    > or returns a low-confidence result (RES-03).
    > Loaded on-demand — not pulled into SKILL.md.

    ## Web playbook defaults

    | Component | Package | Version | Rationale |
    |-----------|---------|---------|-----------|
    | Framework | next.js | 16.2.4 | Full-stack React; App Router default; create-next-app scaffolded |
    | ORM | drizzle-orm | 0.45.2 | 33KB bundle; type-safe; faster cold start than Prisma |
    | Database | postgres | 18-alpine | Multi-user default; compose.yaml service |
    | CSS | tailwindcss | 4.2.4 | Bundled by create-next-app --tailwind; CSS-first in v4 |
    | Package Manager | pnpm | 10.33.2 | Disk-efficient; --use-pnpm flag in create-next-app |

    Supporting packages (not parsed by _read_stack_menu — for reference only):

    | Package | Version | Role |
    |---------|---------|------|
    | drizzle-kit | 0.31.10 | Migrations + codegen (companion to drizzle-orm) |
    | postgres.js | 3.4.9 | Postgres driver for Drizzle (postgres package) |

    ## How stack_researcher.py reads this file

    `_read_stack_menu()` parses rows in the "## Web playbook defaults" table above.
    It maps column 1 (Component) to internal keys: framework, orm, database, css, package_manager.
    Returns `{"framework": {"name": "next.js", "version": "16.2.4", "source": "stack-menu"}, ...}`.
    Returns hardcoded equivalent if this file is absent or unreadable.

    ## Updating defaults

    When a package releases a new major version:
    1. Verify against npm registry: `npm view <pkg> version`
    2. Update the version in the table above
    3. Update RESEARCH.md Standard Stack section to match
    4. Run `python3 -m pytest scripts/tests/ -x` to confirm no test regressions

    ## See also

    - `references/playbooks/web.md` — playbook spec that consumes these defaults
    - `scripts/stack_researcher.py` — implementation that reads this file
    ```

    ===== FILE B: references/question-bank.md =====

    CRITICAL constraints for this file:
    1. JARGON words that MUST NOT appear (word-boundary matched, case-insensitive):
       responsive, ORM, framework, endpoint, middleware, hydration, SSR, SSG, CDN, schema, migration
    2. Every `## Q:` section MUST contain text matching `r"I don.t know,? you decide"` (case-insensitive)
    3. Each question has exactly 3 options: YES path, NO path, "I don't know, you decide" → default
    4. Questions must be outcome-framed plain English — no technical terms

    Check your draft against the jargon list before finalizing. Words to avoid in questions:
    - Do NOT write "responsive" → write "phones and tablets"
    - Do NOT write "ORM" or "database framework" → write "database"
    - Do NOT write "API endpoint" → write "other apps" or "other services"
    - Do NOT write "SSR/SSG/hydration" → never mention these
    - Do NOT write "schema" or "migration" → write "save data" or "track information"
    - Do NOT write "CDN" → write "load quickly for people far away"
    - Do NOT write "middleware" → write "handle logins" or "check permissions"

    ```markdown
    # OSBuilder Question Bank — Web Playbook (v1)

    > Plain-English clarifying questions for the PM role intake flow.
    > Loaded on-demand by SKILL.md. NOT pulled into SKILL.md (≤ 200 line limit).
    > Ask ≤ 5 questions per build. Do not ask all questions on every build.
    > Never use jargon — see "Jargon ban" section below.

    ## Q: Devices

    "Should this work on phones and tablets too, or just on desktop computers?"

    - Yes, phones and tablets too → note in spec: mobile layout needed; Tailwind responsive utilities applied by default
    - Just desktop is fine → note in spec: desktop-only layout; no mobile-specific breakpoints needed
    - I don't know, you decide → YES (phones and tablets — safer default for web apps)

    ## Q: Users

    "Will multiple people use this app with separate accounts, or is it just for you?"

    - Multiple people with their own accounts → note in spec: multi-user auth needed; Postgres via Docker
    - Just for me, no logins → note in spec: single-user mode; local SQLite or simple session
    - I don't know, you decide → Multiple people (multi-user is the safer default for web apps)

    ## Q: Data persistence

    "Do you need to save information between visits — like a list of things, records, or history?"

    - Yes, save data between visits → note in spec: database needed; Postgres via compose.yaml
    - No, it's just for the current visit → note in spec: in-memory or session storage only
    - I don't know, you decide → YES (save data — almost all apps need this)

    ## Q: External access

    "Should other apps or services be able to talk to this app automatically?"

    - Yes, other apps need to connect to it → note in spec: web API path needed; document endpoints in spec
    - No, it's just for people to use in a browser → note in spec: UI-only; no machine-to-machine access needed
    - I don't know, you decide → NO (UI-only — most apps don't need this at first)

    ## Q: File uploads

    "Do users need to upload files — like photos, PDFs, or documents?"

    - Yes, users will upload files → note in spec: file upload handling needed; storage solution required
    - No, no file uploads needed → note in spec: no file handling needed
    - I don't know, you decide → NO (no uploads — add later if needed)

    ## Q: Privacy

    "Should this app be private (only people you invite can use it) or open to anyone?"

    - Private, invite-only → note in spec: auth required; no public signup
    - Open to anyone who finds it → note in spec: public access; consider rate limiting
    - I don't know, you decide → PRIVATE (safer default; easier to open up than to lock down)

    ## Jargon ban

    Never use these words in questions or option text:
    responsive, ORM, framework, endpoint, middleware, hydration,
    SSR, SSG, CDN, schema, migration.

    Plain-English substitutes:
    - "responsive" → "works on phones and tablets"
    - "API endpoint" → "other apps can connect to it"
    - "database schema" → "how data is organized"
    - "migration" → "updating the saved data structure"
    - "CDN" → "loads quickly for people far away"
    ```

    SELF-CHECK before writing: scan every Q section for the JARGON words above. The test uses word-boundary regex — "framework" in "framework" fails; "frameworks" also fails (contains "framework"). Confirm "I don't know, you decide" appears in every ## Q: section.
  </action>
  <verify>
    <automated>cd "/Users/charlie/Documents/Work & Projects/VSCode Projects/OSBuilder" && python3 -m pytest scripts/tests/test_intake.py::test_questions_have_no_jargon scripts/tests/test_intake.py::test_questions_have_you_decide_option -x --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f references/stack-menu.md` exits 0
    - `test -f references/question-bank.md` exits 0
    - `grep -q "## Web playbook defaults" references/stack-menu.md` exits 0 (section header parsed by _read_stack_menu)
    - `grep -q "next.js" references/stack-menu.md` exits 0
    - `grep -q "16.2.4" references/stack-menu.md` exits 0 (verified version)
    - `grep -q "drizzle-orm" references/stack-menu.md` exits 0
    - `grep -q "0.45.2" references/stack-menu.md` exits 0
    - `grep -q "postgres" references/stack-menu.md` exits 0
    - `grep -q "pnpm" references/stack-menu.md` exits 0
    - `grep -q "10.33.2" references/stack-menu.md` exits 0
    - `grep -c "^## Q:" references/question-bank.md` reports ≥ 5 (at least 5 questions)
    - `grep -q "I don't know, you decide" references/question-bank.md` exits 0
    - Jargon check (each must exit NON-zero — word NOT present):
        `grep -wqi "responsive" references/question-bank.md` exits NON-zero
        `grep -wqi "ORM" references/question-bank.md` exits NON-zero
        `grep -wqi "framework" references/question-bank.md` exits NON-zero
        `grep -wqi "endpoint" references/question-bank.md` exits NON-zero
        `grep -wqi "middleware" references/question-bank.md` exits NON-zero
        `grep -wqi "hydration" references/question-bank.md` exits NON-zero
        `grep -wqi "SSR" references/question-bank.md` exits NON-zero
        `grep -wqi "SSG" references/question-bank.md` exits NON-zero
        `grep -wqi "CDN" references/question-bank.md` exits NON-zero
        `grep -wqi "schema" references/question-bank.md` exits NON-zero
        `grep -wqi "migration" references/question-bank.md` exits NON-zero
    - `python3 -m pytest scripts/tests/test_intake.py::test_questions_have_no_jargon -x --tb=short` exits 0 (GREEN)
    - `python3 -m pytest scripts/tests/test_intake.py::test_questions_have_you_decide_option -x --tb=short` exits 0 (GREEN)
    - Stack-menu parse smoke test: `python3 -c "import sys; sys.path.insert(0,'scripts'); import stack_researcher, pathlib; r=stack_researcher._read_stack_menu(pathlib.Path('references')); assert 'framework' in r; assert r['framework']['name']=='next.js'; print('OK')"` prints OK
    - Full suite regression: `python3 -m pytest scripts/tests/ --tb=short` exits 0
  </acceptance_criteria>
  <done>
    references/stack-menu.md exists with "## Web playbook defaults" table parseable by _read_stack_menu(). references/question-bank.md exists with ≥ 5 jargon-free ## Q: sections each containing "I don't know, you decide". Three remaining SKIPPED stubs are now GREEN: test_web_playbook_exists, test_questions_have_no_jargon, test_questions_have_you_decide_option. All 16 Phase 3 test stubs are GREEN (plus 30 existing = 46 total).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| question-bank.md content → SKILL.md PM role | Questions are loaded into the LLM context. Must not contain injected instructions or misleading directives. Defense: content is plain-English questions with defined option structure. |
| stack-menu.md content → stack_researcher.py | Reference file content is parsed as structured data. Malformed content falls back to hardcoded defaults — never crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-13 | Tampering | question-bank.md — jargon in questions | mitigate | test_questions_have_no_jargon enforces the JARGON set via word-boundary regex; acceptance criterion lists 11 word-specific grep checks; any jargon word added in future edits will fail the test |
| T-3-14 | Tampering | stack-menu.md — wrong version triggers wrong stack | accept | Versions in this file are the fallback only (when brainiac times out); brainiac provides fresher data at runtime; the file version is the "known good" baseline verified against npm registry on 2026-04-30; drift accepted until next explicit update |
| T-3-15 | Information Disclosure | question-bank.md — sensitive project info in question context | accept | Questions ask about generic app properties (devices, users, files); no PII collected; answer context is written to derived_spec.md which is a local file, not transmitted externally |
| T-3-16 | Tampering | question-bank.md — "you decide" defaults influence build | accept | Defaults are documented explicitly in each Q section and in the jargon-ban section; defaults favor safety (private, multi-user, save data); no default exposes the user to a worse security posture than they had before |
</threat_model>

<verification>
After both tasks complete:
1. `python3 -m pytest scripts/tests/test_scaffold_dispatch.py::test_web_playbook_exists -x --tb=short` exits 0 (GREEN — was SKIPPED).
2. `python3 -m pytest scripts/tests/test_intake.py::test_questions_have_no_jargon -x --tb=short` exits 0 (GREEN — was SKIPPED).
3. `python3 -m pytest scripts/tests/test_intake.py::test_questions_have_you_decide_option -x --tb=short` exits 0 (GREEN — was SKIPPED).
4. `python3 -m pytest scripts/tests/ --tb=short` exits 0 — all 46 tests GREEN or SKIPPED (0 FAIL).
5. `python3 -c "import sys; sys.path.insert(0,'scripts'); import stack_researcher, pathlib; r=stack_researcher._read_stack_menu(pathlib.Path('references')); assert r['framework']['name']=='next.js'; assert r['package_manager']['name']=='pnpm'; print('OK')"` prints OK.
6. `grep -c "^## Q:" references/question-bank.md` reports ≥ 5.
</verification>

<success_criteria>
- references/playbooks/web.md: scaffold command (7 flags), 4 post-scaffold files, forbidden files list, refuse list, pinned versions — test_web_playbook_exists GREEN
- references/stack-menu.md: "## Web playbook defaults" table parseable by _read_stack_menu(); all 5 components with verified versions — fallback smoke test passes
- references/question-bank.md: ≥ 5 ## Q: sections, zero jargon words, every section has "I don't know, you decide" — test_questions_have_no_jargon and test_questions_have_you_decide_option GREEN
- Full test suite: 46/46 tests GREEN (0 FAIL, some SKIPPED if any stubs remain untriggered)
- Phase 3 Wave 0 validation gate: ≥ 46 tests collected, all GREEN or SKIPPED
</success_criteria>

<output>
After completion, create `.planning/phases/03-intake-stack-research-web-playbook-one-playbook-e2e/03-05-SUMMARY.md` with: tasks completed, 3 SKIPPED stubs flipped GREEN, final test count (46/46 GREEN), jargon check passed, stack-menu parse confirmed. Note that the Phase 3 `/gsd-verify-phase 3` gate is now unblocked: all 16 Phase 3 stubs are GREEN and 03-VALIDATION.md can be set to `nyquist_compliant: true`.
</output>
